# 07 · 路线图

**赛程剩余**：距决赛集放出（2026-10-22）**30 天**，距最终截止（2026-11-05）**44 天**。

**go/no-go 门已经过了**：[F22](02-findings.md#f22) 实测 $h_{\text{cross}}/h_{\text{replicate}}$
= **0.708**，远高于 35% 的门。下面「唯一的 go/no-go 门」一节是历史记录，不再是待办。

---

## 当前位置：官方刻度 **0.1508 ± 0.042**（V13），榜首 0.1899，缺 0.0391 —— **差小于棒**

⚠️ 这一节的所有数字用**官方 $b$/$r$**（[T9](06-traps.md#t9) 的教训）。
旧的自建退化基线数字（V8 = 0.2025）**不可与榜首比较**，见 [F32](02-findings.md#f32)。

⚠️ **0.0391 这个缺口小于 8 扰动面板的采样棒 0.0423**（[F36](02-findings.md#f36)：
`P(V8 > 榜首) = 0.0885`，90% 区间覆盖 0.1899）。下表的逐成员排序**在本面板上是确定的**，
但它对决赛轮（300 扰动 × 3 context，全新细胞系）只是**弱证据**。
尤其：V13 相对 V8 的 +0.0293 **全部**落在两个不可迁移的成员里，
四成员可迁移核心上 V8/V13/V14 相差 **0.12 个 sd**，即持平。

| 优先级 | 指标 | V13 lo | 到榜首的差 | 状态（2026-09-23 收盘） |
|---|---|---|---|---|
| **守** | `pds_cosine` | **0.8782** | **领先 +0.1702** | [F34](02-findings.md#f34)：V13 把 raw 推到 **0.8750**（榜首 0.820）。⚠️ **但这是最不可迁移的一项**，见下方尺度警告 |
| **守** | `sig_jaccard` | 0.0770 | **领先 +0.0810** | 全场 315 队此项为 0。已知**有敞口**（打分器自己重算显著集，V13 动了 1.3e-5），每次改动必须逐位核对 |
| ~~P0~~ **封闭** | `direction_reach` | **−0.0241** | +0.2371 | **不可赎回**（[F35](02-findings.md#f35)）。V14 压幅实测 0.0250 → **0.0208**（更低）；V8 的 0.1482 有 84% 是 MAT2A 一个扰动的重归一符号巧合（池∩召集集 = **0**）；有意去做全局符号的 **oracle 上界 0.1421 < V8 的 0.1482**。与 `nmae` 同一个根因 |
| **P1** | `lfc_nmae` | −0.0168 | +0.1948 | **当前不可达**（[F33](02-findings.md#f33)）：符号不可迁移，一致率 51–57%。[F35](02-findings.md#f35) 按 $z$ 分层补测：0–90 分位恒为 ~0.511，只有最高 0.5% 分位（15 个基因）到 0.800 |
| **P2 ← 现在是最大的可做项** | `expr_mse_capped_norm` | 0.0000 | +0.0410 | V13 把 raw 从 1.0819 推到 **2.2208**，距官方 $b$（≈0.9985）从 4.7% 拉到 **122%**。V14 回到 1.3366（仍 +34%）。这是唯一一个**已知有旋钮**（$k_{\text{off}}$ / $\lambda_{\text{off}}$）且与 `pds` 在同一条曲线上的成员 |
| P3 | `direction_fidelity` | −0.0092 | +0.0122 | 被 $\mathrm{fid}=0.5+(a-0.5)h$ 钉死（[F15](02-findings.md#f15)），无独立旋钮 |

### 诚实的算术（已按 [F35](02-findings.md#f35) 重写；旧版建立在「V14 赎回 reach」上，那个前提是错的）

`reach` 封闭之后，缺口的构成变了。V13 逐成员实测（官方 lo，除 6）：

$$\underbrace{0.1464}_{\texttt{pds}} + \underbrace{0.0128}_{\texttt{jac}}
\underbrace{-0.0028}_{\texttt{nmae}} \underbrace{-0.0015}_{\texttt{fid}}
\underbrace{-0.0040}_{\texttt{reach}} + \underbrace{0}_{\texttt{mse}} = \mathbf{0.1508}$$

到 0.1899 还缺 **0.0391**。可做的只剩两项，且它们**在同一条曲线上**：

| 项 | 满仓头寸（除 6 后） | 可做性 |
|---|---|---|
| `mse` | +0.0068 | **有旋钮**：raw 须从 2.2208 跌破 0.9985。$k_{\text{off}}$ / $\lambda_{\text{off}}$ 同时决定 `pds` 与 `mse`，尚未扫过这条**两成员**曲线 |
| `reach` + `nmae` | +0.0720 | **都锁在「跨系可迁移的 lfc 符号」这一个前提上**（F33 + F35）。没有新符号源之前，解码器旋钮对两项都恒等于 0 |

> **两句话的结论：**
> ① 解码器侧还没扫的只有 $(k_{\text{off}}, \lambda_{\text{off}})$ 对 `pds`/`mse` 的**联合**曲线，
> 满仓也只值 +0.0068 —— 不足以闭合 0.0391。
> ② 真正的 P0 是**去找符号可迁移的信号**
> （Replogle 2022 K562+RPE1 的成对结构、Jiang 2025 六系，都还没被当作 lfc **符号**源验证过）。
> 这一条没有进展之前，0.0391 的缺口在本地是闭不上的 —— 继续调旋钮属于 [F29](02-findings.md#f29) 那类归因幻觉。

---

## ⚠️ 尺度警告：最大的收益落在最不可迁移的指标上

[E30/RESULT.md](../experiments/E30-scale/RESULT.md) 实测三条，全部改变上表的读法：

**1. `pds_cosine` 是面板相对的秩指标，不能跨面板搬。**
`discrimination.py:215` 用 $D = n-1$ 对面板内非对照扰动取分母，`:248` 把**每一个**面板
靶基因从两侧操作数里剔掉（`exclusion_scope="panel"`）。**分母与剔除集都依赖面板的大小与成员。**
我们的 pds = 50/56，意思是 8 个扰动里多数排第 1 —— 在 300 个扰动的面板上这是完全不同的任务。
**V13 的 +0.2927 是这批数字里最可能在放大后蒸发的一项。**

**2. context 轴（1 vs 3）本地永不可闭合。**
`context_A/B/C.h5ad` 各 18,400 细胞**全部是对照**（`target_gene` 只有 1 个类别
`non-targeting`）；manifest 声称的 `ground_truth_cells: 138400` 里那 120,000 个扰动细胞
**三个文件里都没有**。我们自己的 `adata_Validation.h5ad` 连 cell-line 列都没有
（`batch` 的 48 个类别是 10x Flex 测序批次）。且基因空间不同（18,533 vs 18,080）。
**要等组织者 2026-10-22 放出决赛集，而那是全新的 D/E/F + 另外 300 个基因。**

**3. 扰动轴（8 vs 300）本地天花板是 41 个，13.7%。**
约束不是磁盘或时间，而是同时满足三条的扰动只有 41 个：在验证集里、在 K562GW 源里、
且有 ≥ 400 个真实细胞。41 个扰动 ≈ 34,800 细胞 ≈ 470 MB，`compute_metrics` 约 1.8 h/侧。

**4. 误差棒已实测（[F36](02-findings.md#f36)，2026-09-23）：官方 lo 是 0.1215 ± 0.0423。**
E30 Part 1 跑完了（`build_c1.py build pred` 198 s → `score_scale.py metrics pred_8` 1530 s
→ `analyze` 8 s，B = 2000，$b$ 固定）。重建行对 `agg_v8.parquet` **逐位相同**。三条后果：

- **榜首的差小于这个棒。** `P(V8 > 0.1899) = 0.0885`，90% 区间 [0.0680, 0.2022] **覆盖**
  0.1899。不是「我们持平」（他们测在 300×3 上，棒小得多），而是**8 扰动面板分辨不出
  0.039**。剔掉 GNG12 一个扰动就动 +0.0287，而 V13 相对 V8 的全部增益是 +0.0293。
- **「V13 比 V8 高 +0.0293」在迁移意义上不成立。** 两者最大的成员**都**不可迁移：
  V8 的 `reach` +0.1110 是巧合（F35），V13 的 `pds` +0.2927 是面板相对（本节第 1 条）。
  去掉这两个，四成员可迁移核心是 V8 0.0081 / V13 0.0127 / V14 0.0137，
  棒 **0.0389** —— 相差 **0.12 个 sd**。**三个配置的可迁移内容持平。**
- **V13 的采纳是押注 `pds` 保留率：** 需 ≥ 39.9%（lo）/ ≥ 48.6%（hi）才真的强于 V8。
  **V14 无条件被 V8 支配**（需 +0.1177，其 `pds` 增益只有 +0.0836）。

> **下一步的优先级由这一节决定，不由上面的缺口表决定：**
> ~~① E30 Part 1 拿误差棒~~ `已完成 → F36`
> ② **41 扰动面板上重测 V8 vs V13** —— 唯一能做的放大检验，现在有了**明确的判据**：
> 只需看 `pds` 的 8→41 优势保留率是否 ≥ 40%。低于 40% 就把基线退回 V8。
> ③ 找符号可迁移的信号（F33 + F35 + F36 ⑤ 三条独立指向同一处）。
> **解码器旋钮已经出局**：它能动的只有 `nmae`/`fid`/`jac`/`mse` 四项，
> 而这四项在 0.039 的棒里全部持平（F36 ③）。

### 提交合法性（与分数无关，但是硬门）

官方打分器对 V6/V7/V8 全部告警：`design_cells` 的 `V *= tgt / V.mean(0)`
把经验均值钉死，是**可检测的伪影**（[F30](02-findings.md#f30)）。
`decoder.py` 已加 `force_mean` 开关，**E31 的 V9（`force_mean=False`）必须测出代价**
才能定最终提交用哪一条。这件事不能留到最后一周。

---

## ~~唯一的 go/no-go 门~~ `历史记录 · 已由 F22 答出 0.708，门已通过`

三个理论确认（[F15](02-findings.md#f15)）把整个方向压缩成一个数：

$$\boxed{h \;=\; \frac{|R_p \cap \hat R_p|}{|R_p|} \;\ge\; 0.134 \ \text{才追平榜首}}$$

而且已确认：

- **物理上限** $h_{\text{replicate}} = 0.570$ —— 同系重做实验也只重叠 57%
- **零生物学上限** $h = 0.130$ —— 只按可检出性排序赢不了（理论确认，不用做实验）
- **`fid` 不能绕过** $h$ —— $\mathrm{fid}=0.5+(a_{\text{true}}-0.5)h$，假阳性方向被钉在 0.5

所以只剩一个问题，而它**完全用公开数据就能答**：

### E07 · 跨细胞系响应基因集重叠率 ← **P0，唯一的门**

用公开多细胞系 Perturb-seq 测：同一个基因被敲低，在细胞系 A 的响应基因集与在细胞系 B 的重叠多少？

**素材**（都有同一批扰动跨多个细胞系）：

| 数据集 | 细胞系 | 共享扰动 |
|---|---|---|
| Replogle 2022 | K562 + RPE1 | 数千个 |
| Jiang 2025 | A549 · MCF7 · HT29 · HAP1 · BxPC3 · K562 | 六系，结构与本届同构 |
| Nadig 2025 | HepG2 + Jurkat | common essential |

**必须做的实验设计控制**（否则测出的数偏高、不可用）：

1. **降采样到本届条件** —— 每扰动 400 个细胞、中位 ~20,000 UMI。
   Replogle 的功效远高于此，不控制会高估重叠率。
2. **同一套判定** —— 5 CPM 门 + Wilcoxon + 每扰动内 BH，用 `src/vcclab/scorer.py`。
3. **同时测同系 replicate 重叠** —— 每个细胞系内部对半劈开，作为分母。
   报的是**比值** $h_{\text{cross}} / h_{\text{replicate}}$，不是绝对值。

**判定门**：

| $h_{\text{cross}} / h_{\text{replicate}}$ | 结论 |
|---|---|
| **≥ 35%** | 计划成立，目标 1.27× 榜首以上，全速推进 Stage 1 |
| **23% – 35%** | 勉强追平，必须把 `pds`/`mse` 也一起做才有余量 |
| **< 23%** | **DE 半边没有可迁移信号。** 转向只打 `pds`/`mse`，或承认这题需要大模型 |

**在 E07 出数之前，不应该往 Stage 1 的实现上投入**（`transfer.py`、`calibration.py` 那些）——
那是在赌一个还没验的前提。

---

## 降级与保留

| 实验 | 原定位 | 现定位 | 理由 |
|---|---|---|---|
| [E05](../experiments/E05-detectability-only-baseline/) | go/no-go，可能直接夺冠 | **理论校准检查** | 理论上限 $h=0.130$ < 追平所需 0.134。跑它只为验证第 ② 项理论计算 |
| [E06](../experiments/E06-coexpression-sign/) | P0 | **P1** | 只影响 $a_{\text{true}}$（0.9 已是乐观值），不影响 $h$ —— 而一切走 $h$ |
| [E08](../experiments/E08-callset-size/) | P0 | **已完成（解析 + 数值）** | $K^\ast=287$ vs 解析 288，偏差 0% |

---

## E07 之后（条件性）

### 若 ≥ 35%：实现 Stage 1

按 [05-stage1-design](05-stage1-design.md) 的装配图：

- [x] `detectability.py` · `shrinkage.py` · `probit.py`
- [ ] `transfer.py` —— 源域平均谱 + 维度轴转置的协方差（$p$ = 细胞系，不是基因）
- [ ] `calibration.py` —— OOB envelope + misfit_ratio 降级（bnode 公式）
- [ ] `callset.py` —— 两级阈值 + half-support 自适应 $K$
- [ ] `stage1.py` —— 装配

源域数据（按优先级）：

| 数据集 | 用途 | 原始 | 平均谱 |
|---|---|---|---|
| Replogle 2022 K562 全基因组 | 300 个 target 的实测方向主来源 | 61.3 GB | 731 MB |
| Replogle 2022 RPE1 | 第二个源细胞系 | 8.1 GB | 152 MB |
| Nadig 2025 HepG2 + Jurkat | 再加两个源细胞系 | 13.9 GB | ~200 MB |
| Jiang 2025 六系 | **离线练习场** + E07 的主素材 | — | ~300 MB |
| 2025 VCC H1 | 同化学同流程，E05 校准检查 | 中 | — |

### 若 < 23%：转向

只打 `pds`（榜首 scaled 0.708，说明大模型有效）与 `mse`。
这条路我们没有优势，但 Stage 2 解码器对任何路线都仍然必需。

---

## 线上探针（谨慎，只花该花的）

| 目的 | 设计 | 花费 |
|---|---|---|
| 确认 $\mathbb{E}\lvert R_p\rvert$ | 令 $\hat R$ = 全部门内基因，回读 `jac` 原始分 $=\lvert R_p\rvert/9{,}929$ | 1 次（保险，虽然 [F8](02-findings.md#f8) 已用两个锚点推出 288） |
| 定最优收缩系数 $\lambda$ | `nmae` 关于 $\lambda$ 凸且分段线性 → 3 个值包围 | 3 次 |

**纪律**：只提取可迁移的超参数，不要记忆某个集合。决赛轮是不同细胞系 + 不同 panel。

---

## 三个决策点

**① 什么时候上线提交？** 本地分数创新高，或套取未知量。其余全在本地（10 分钟一轮）。

**② 用什么当离线基准？** Jiang 2025 六个癌系做替身 —— 留三个当「A/B/C」，
自建 $b$/$r$ 锚点（`cell-eval2` 的 `baseline.py`、`ceiling.py`、`anchor.py`、`scales.py` 都公开），
跑同一套打分逻辑。

**③ 决赛轮怎么切？** 10-22 放出 D/E/F，只需重跑建 ψ 表那一步。
**标签绝对不能沿用 A/B/C**（[T1](06-traps.md#t1)）。

---

## 待回答的开放问题

1. **$h_{\text{cross}}/h_{\text{replicate}}$ 是多少？** ← E07，唯一的门
2. **共享响应主轴占多少方差？** 如果占比高，$h$ 的下限会被它托住
3. **$\alpha^\ast$ 会自适应到哪一侧？** AdaptiveEROP 自己的 ablation 显示 $n_c$ 小时分层层会变差；
   $n_c=3\sim8$ 正在那个 regime 边上
4. **MDE 分布在决赛轮细胞系上是否类似？** 6.8× 是 context A 的实测值（BH 阈值）。
   D/E/F 是全新细胞系 —— 10-22 放出后第一件事就是重算
