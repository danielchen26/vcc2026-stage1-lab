# 02 · 实测发现

编号的事实清单。每条标注**来源**与**溯源等级**：

- `官方` — Arc 官方页面 / metric spec / `manifest.json`
- `实测` — 本机在官方 `controls.zip` 上跑出来（Apple M1 Pro / 16 GB / 无 CUDA，2026-08-27）
- `推算` — 由官方 + 实测做算术推出

工具版本：`cell-eval2` 0.16.0 · `vcc-cli` 0.1.0 · numpy 2.5.2 · scipy 1.18.1 · anndata 0.13.3

---

## F1 · 数据包逐项与规格吻合 `实测`

| 核对项 | 实测 | 规格 |
|---|---|---|
| 矩阵形状 | 18,400 细胞 × 18,533 基因，CSR float32，109,910,478 个非零 | ✓ |
| 基因顺序 | 三个文件都与 `gene_names.csv` **逐位相同** | ✓ |
| 数值 | 全整数，min 1，max 977 | ✓ raw counts |
| 测序深度 | 中位 **20,109** · 均值 21,134 · [3,275, 52,420] | ✓「median ~20,000」 |
| 每细胞检出基因 | 中位 6,147 · 均值 **5,973** | — |
| 对照 guide | 46 条，各恰好 400 细胞 | ✓ |
| 待预测基因 | 300 行，无重复，全部在基因表内 | ✓ |

来源：`experiments/E04-official-parity/` 的加载步骤。

---

## F2 · 发布的对照细胞就是打分用的比较组 `官方`

`manifest.json`：

```json
"per_context": { "A": { "control_cells": 18400, "ground_truth_cells": 138400, "n_ntc_ids": 46 } }
```

$$138{,}400 \;=\; 300\times400 \;+\; 18{,}400$$

**这一条把后面所有事情解锁了。** 对照分布、5 CPM 筛选门、多重检验的基因全集，
全部可以在本地精确重建。

---

## F3 · Wilcoxon 秩和对固定对照组有闭式 `实测`

$$\psi_g(v)=\#\{c: x_{cg}<v\}+\tfrac12\#\{c: x_{cg}=v\},\qquad U_g=\sum_{i=1}^{400}\psi_g(v_i)$$

与 `scipy.stats.mannwhitneyu` 逐位相等：

| 情形 | scipy 的 U | ψ 求和 |
|---|---|---|
| 正常随机分布 | 3,862,696.0 | 3,862,696.0 |
| 整体偏移 | 4,735,458.5 | 4,735,458.5 |
| **400 个细胞全相同** | 4,771,000.0 | 4,771,000.0 |

3/3 精确相等。来源：[E01](../experiments/E01-psi-identity/)。

**推论**（`推算`）：六个指标全部只经过每基因两个标量。细胞级联合分布对分数零贡献。

---

## F4 · 显著性阈值极薄 `推算`

$$d_{\text{crit}}=\frac{1.96\,\sigma_{\text{tie}}}{n_1n_2}=0.0278 \qquad (\sigma_{\text{tie}}=104{,}477)$$

平均对照分位数只要移动 **2.78 个百分点**就判显著。

---

## F5 · 显著性与方向完全解耦 `实测`

固定一阶矩（真实基因 `SELENOT`，context A，对照均值 62.54 CPM，5,885/18,400 个对照细胞检不到它，
目标均值锁在 78.18 CPM 即 lfc = +0.322），只改组内分布形状：

| t | 非零细胞 | 均值 | $d$ | $p$ | 判定 |
|---|---|---|---|---|---|
| 0.00 | 400 | 78.18 | +0.1614 | 2.81e-29 | 显著 |
| 0.60 | 250 | 78.18 | +0.0944 | 4.61e-11 | 显著 |
| 1.05 | 195 | 78.18 | +0.0348 | 1.51e-02 | 显著 |
| **1.30** | 174 | 78.18 | +0.0043 | **7.66e-01** | **不显著** |
| 1.70 | 148 | 78.18 | −0.0391 | 6.32e-03 | 显著 |
| **3.00** | 100 | 78.18 | **−0.1312** | 5.20e-20 | 显著 |

**关键观察：t = 3.00 时均值仍然向上（lfc = +0.32），但检验读到的偏移变成了负的。**
检验读**秩**，倍数变化读**均值**，两者可以指向相反方向。

来源：[E02](../experiments/E02-dispersion-dial/)。完整 12 行表见该目录。

**推论**：每个基因上有**三个可独立指定的旋钮** —— 进不进显著集、报涨还是报跌、报涨多少。

---

## F6 · 可检出性门槛：用 BH 有效阈值算 `实测`

### 阈值必须用 BH 的，不是单基因 α=0.05

真实打分器在**每个扰动内**、对 gate 内 $m=9{,}929$ 个基因做 Benjamini–Hochberg。
在解点 $k=|R_p|\approx288$ 处，$p$ 值截断为

$$p_{\text{BH}} = \frac{\alpha k}{m} = \frac{0.05\times288}{9929} = 0.00145
\quad\Longrightarrow\quad z_{\text{BH}} = 3.184$$

用单基因 α=0.05 的 $z=1.96$ 会把门槛**系统性低估**。而且这不是纯尺度变换 ——
低门槛基因被推高更多（ψ→lfc 关系饱和）：

| 分位 | **BH（正确）** | α=0.05（错） | 比 | BH 下的倍数变化 |
|---|---|---|---|---|
| 5% | **0.0985** | 0.0220 | 4.48× | 1.07× |
| 25% | **0.1845** | 0.1021 | 1.81× | 1.14× |
| 50% | **0.2980** | 0.1869 | 1.59× | **1.23×** |
| 75% | **0.5320** | 0.3693 | 1.44× | 1.45× |
| 95% | **1.2586** | 1.0679 | 1.18× | 2.39× |

$$\text{动态范围} = \frac{p_{95}}{p_{25}} = \mathbf{6.8\times}\qquad (p_{95}/p_5 = 12.8\times)$$

一类错误噪声底从 3.31% 降到 **0.07%** —— 这正是 BH 控 FDR 应有的效果。

> **更正记录**：曾先后报过 33×（用 $p_{95}/p_5$，分母落在噪声底上）和 10.5×（用了错的 α=0.05 阈值）。
> 现值 **6.8×** 用的是打分器真实的 BH 有效阈值。

### 推论，以及它的边界

$$R_p = \underbrace{\{h:\ \text{生物上真的变了}\}}_{\text{未知，需要建模}} \cap \underbrace{\{h:\ \text{检得出来}\}}_{\text{官方已经给你了}}$$

**但可检出性只是边际修正项，不是策略** —— 见 [F15](#f15) 的理论确认：
只按门槛排序的「零生物学」策略理论上限只到 $h=0.130$，而追平榜首需要 $h\ge0.134$。

来源：`scripts/export_app_data.py`，全量 9,929 基因，19.6 s，含显式 lfc=0 评估。

---

## F7 · 「预测什么都没变」会被判 99.3% 的基因「变了」 `实测`

一个把对照平均谱复制 400 遍的退化提交（lfc 中位绝对值 **0.0052**，即「什么都没变」），
被判 **9,863 / 9,929 = 99.3%** 的基因「变了」。

**这是解码器故障，不是选集故障。** 明明预测「没变」，却报了 9,863 个「变了」。

---

## F8 · 由此白拿参考显著集的大小 `推算`

既然任何零方差的 400 细胞提交都会吞掉几乎整个筛选门（F7），那么官方的 `jac` 基线满足

$$b_{\text{jac}} \;\approx\; \frac{|R_p \cap \hat R|}{|R_p \cup \hat R|} \;\approx\; \frac{\mathbb{E}|R_p|}{9{,}863}$$

官方公布 $b_{\text{jac}} = 0.021\text{–}0.037$，代回：

$$\boxed{\ \mathbb{E}|R_p| \;\approx\; 200 \sim 375\ \text{个基因（中点约 288）}\ }$$

**这个数直接给出 `fid`/`jac` 联立最优点上该报多少个基因。** 本来打算花一次线上提交去套。

⚠️ 依赖「官方基线也是退化提交」这一步。与 `mse` 基线 ≈ 1（官方 0.986–0.992，
即「emit control unchanged」）一致，故置信较高，但仍是推算。

---

## F9 · 三个细胞系是跨组织的，不是近亲 `实测`

basal log-pseudobulk 两两 Pearson：**A–B 0.7420 · A–C 0.6854 · B–C 0.7927**。

筛选门也是细胞系特异的：A 9,929 / B 9,626 / C 10,124，交集 7,991、并集 11,961
—— 约 4,000 个基因只在部分细胞系里被检验。

**推论**：跨细胞系迁移是真问题，Stage 1 不能靠「假装三个细胞系一样」蒙过去。

---

## F10 · Stage 2 构造器是无损的 `实测`

| | |
|---|---|
| 意图让 250 个基因显著 → 实际显著 | **250 / 250** |
| 召回 / 精确率 | 100% / 100% |
| 假阳性 | **0** |
| 方向一致率 | 100% |
| lfc 中位绝对误差 | 6.1e-4 |
| 单组耗时 | **0.28 s** |
| 每细胞非零值 | 5,998（真实数据 ~5,973，天然合规） |

完美 Stage 1 假设下送进评分公式，得到的正好是「完美复现真实数据」的理论上限：
`jac` scaled **2.62** · `fid` **1.62** · `nmae` **1.66**。

来源：[E03](../experiments/E03-decoder-exactness/)。

---

## F11 · 与官方打分器逐基因一致 `实测`

`cell-eval2` 0.16.0，preset `vcc2026`，scanpy 后端，CPU：

| | 官方 | 我们 |
|---|---|---|
| 门内基因数 | 9,929 | **9,929（完全一致）** |
| 官方 DE 表 | 29,787 行 | — |
| `log2_fold_change` 最大绝对差 | — | **1.0e-5**（float32 存储精度） |
| `log10(p_adj)` 中位绝对差 | — | **0.0000** |

显著集大小：

| 组 | 官方 | 我们（含并列校正） | 我们（未校正） | 对称差 |
|---|---|---|---|---|
| ABCD1 | 251 | **251** | 249 | **0** |
| ACLY | 250 | **250** | 249 | **0** |
| ADNP | 251 | **251** | 250 | **0** |

官方六指标同时给出：`de_wilcoxon_sig_jaccard` = 0.249377（三组相同，= 100/401，
与我们故意设计的 100 个重叠吻合）；`direction_fidelity` = 0.49004 / 0.466135 / 0.517928；
`direction_reach` = 0（方向随机设，纯度到不了 0.9，行为正确）。

来源：[E04](../experiments/E04-official-parity/)。

---

## F12 · 41 倍加速及其机制 `实测` + `推算`

| | 3 个扰动（单侧 DE 表） | 全 panel（300 × 3） |
|---|---|---|
| 官方 `cell-eval2`（scanpy CPU） | 296.85 s | **52 小时** |
| ψ 复刻 | **7.66 s** | 60 min 单核 / **6 min 十核** |

实测比 **38.8×**。机制（op 计数）：

| | 每扰动每基因的比较次数 |
|---|---|
| 官方 | $(n_1{+}n_2)\log_2(n_1{+}n_2) = 18{,}800\times14.2 = \mathbf{266{,}960}$ |
| 我们 | $n_1\log_2 n_2 = 400\times14.2 = \mathbf{5{,}668}$ |

理论比 **47.1×**；一次性建表成本（$2.59\times10^9$ 次比较，实测 **2.0 秒**）在 900 组上摊销后
只占我们总工作量的 **4.9%**，故全 panel 摊销后趋近 **44.8×**。

---

## F13 · 提交矩阵的密度与内存 `实测`

| | |
|---|---|
| 每细胞非零值 | 5,998 |
| 总存储条目 | 2.16×10⁹ = 上限（4.75×10⁹）的 **45%** |
| 原始内存 | **17.1 GB** → 装不进 16 GB |
| 按组流式写 | 每块 400 × 5,998 ≈ 19 MB，常驻 < 100 MB |
| 生成整份提交 | **4 分钟**（900 × 0.28 s） |

**迭代时不需要写盘** —— 自评可以逐组在内存里算完就丢。只有真要提交时才落盘。
这是「10 分钟一轮」成立的前提。

清零 <1 CPM 基因几乎不省 nnz（5,998 → 5,986），因为稀疏度来自自举的真实细胞支撑集。

---

## F14 · 收益算术：一切走 $h$ `理论确认`

**定义** $h = |R_p \cap \hat R_p| / |R_p|$，即我们能恢复的参考响应基因比例。

### `fid` 不是自由的 —— 它由 $h$ 完全决定

`fid` 数的是「你报的基因里，预测 lfc 符号与**参考的** lfc 符号一致」的比例。
对一个**没有真实效应**的基因，参考测到的 lfc 是噪声主导 → 符号是掷硬币 → **不可预测**。
所以假阳性上的方向准确率被结构性钉在 $\approx 0.5$：

$$\mathrm{fid} = a_{\text{true}}\cdot h + 0.5\cdot(1-h) = 0.5 + (a_{\text{true}}-0.5)\,h$$

> **更正记录**：曾把 $a_{\text{false}}$ 当自由参数（取 0.55–0.85），据此声称
> 「$h=0$、只把方向做对就能到 1.20× 榜首」。**那是错的** —— $h=0$ 时 $\mathrm{fid}$ 必然 $=0.5$。

### 修正后的收益表

固定 `pds` raw = 0.820、`mse` raw = 0.959（榜首水平），$a_{\text{true}}=0.90$，
`reach` $\approx 0.8h$，`nmae` $=1-0.5h$：

| $h$ | fid raw | fid scaled | jac scaled | **overall** | vs 榜首 |
|---|---|---|---|---|---|
| 0.00 | 0.500 | −0.043 | −0.078 | 0.0891 | 0.47× |
| 0.10 | 0.540 | 0.090 | 0.064 | 0.1638 | 0.86× |
| **0.134** | 0.554 | 0.135 | 0.115 | **0.1899** | **1.00×** |
| 0.20 | 0.580 | 0.223 | 0.222 | 0.2411 | 1.27× |
| 0.30 | 0.620 | 0.357 | 0.399 | 0.3215 | 1.69× |
| 0.40 | 0.660 | 0.490 | 0.597 | 0.4056 | 2.14× |
| 0.50 | 0.700 | 0.623 | 0.823 | 0.4941 | 2.60× |

$$\boxed{\text{追平榜首需要 } h \ge 0.134}$$

---

## F15 · 三个理论确认（不需要新数据） `理论确认`

### ① $h$ 的物理上限：0.570

用官方 replicate 锚点反推：$r_{\text{jac}}=0.399$，而 $\mathrm{jac}=h/(2-h)$，故

$$h_{\text{replicate}} = \frac{2\times0.399}{1.399} = \mathbf{0.570}$$

**连同一细胞系、同一扰动、真实数据对半劈开，显著集也只重叠 57%。** 这是噪声上限。

| 目标 | 需要 $h$ | = replicate 水平的 |
|---|---|---|
| 追平榜首 | 0.134 | **23%** |
| 1.27× 榜首 | 0.20 | 35% |
| 1.69× 榜首 | 0.30 | 53% |

### ② 「零生物学」策略的理论上限：$h = 0.130$，赢不了

只按可检出性排序、报 $K=288$ 个基因。关键化简：先验响应概率 $\pi$ 在分子分母里**约掉**，
所以 $h$ 只依赖效应量尺度 $s$。设 $|\mathrm{lfc}|\sim\mathrm{Exp}(s)$，
用实测 MDE 分布 + 约束 $\mathbb{E}|R_p|=288$ 定出 $\pi$，扫遍可行区间：

| $s$ | 蕴含 $\pi$ | 理论 $h$ | jac scaled |
|---|---|---|---|
| 0.15 | 0.148 | **0.130** | 0.11 |
| 0.25 | 0.096 | 0.091 | 0.05 |
| 0.40 | 0.070 | 0.069 | 0.02 |
| 0.60 | 0.057 | 0.058 | 0.00 |
| 0.90 | 0.049 | 0.050 | −0.01 |
| 1.40 | 0.043 | 0.044 | −0.02 |

$h\in[0.044,\,0.130]$，最好情况 overall $=0.1867 = 0.98\times$ 榜首。**赢不了。**

**机制**：多数真实效应（$s\approx0.4$–0.6）本来就远高于多数基因的门槛，
可检出性只在小效应尾巴上起作用。

> **更正记录**：曾把「E05 零生物学基线」列为可能直接夺冠的 go/no-go 门。**理论上限就到不了。**
> E05 降级为理论校准检查。

### ③ 因此真正的问题只有一个

$$\text{跨细胞系迁移能恢复的响应基因集，是否达到同系重做实验的 23\% 以上？}$$

这个问题**完全用公开的多细胞系 Perturb-seq 数据就能回答** ——
不需要官方数据、不需要提交额度。见 [E07](../experiments/E07-source-coverage/)。

---

## F16 · 跨细胞系迁移有真信号，但只在必需基因上测得 `实测`

[E07](../experiments/E07-source-coverage/)：Nadig et al. 2025 四细胞系
（K562/RPE1/Jurkat/HepG2），2,052 个共享扰动。

| 问的问题 | 用哪个数 | 值 | 折算 $h$ | 对榜首余量 |
|---|---|---|---|---|
| 扰动特异性生物学是否迁移？（**科学**） | 扣置换基线 | 0.430 | 0.245 | 1.83× |
| $R_p$ 里有多少能被恢复？（**比赛**） | 原始比值 | **0.625** | **0.356** | **2.66×** |

打分只看集合成员，**不管一个基因为什么在 $R_p$ 里** —— 枢纽基因照样算分。
所以比赛问题该用 0.625。

同系 replicate 分母用 **data fission** 构造：给定 $\hat\beta \sim N(\beta,\sigma^2)$
且 $\sigma$ 已知（`lfcSE`），取 $Z\sim N(0,1)$，则 $\hat\beta_{1,2} = \hat\beta \pm \sigma Z$
的 $\mathrm{Cov} = \sigma^2 - \sigma^2 = 0$ 且联合正态故**独立**；各自 $\mathrm{Var}=2\sigma^2$
即 SE 放大 $\sqrt2$，正好等价于样本量减半，与官方 split-half 锚点同构。
实测 $h_{\text{replicate}} = 0.381$–$0.458$ vs 官方锚点 0.570，同量级略低，分母机器可信。

---

## F17 · 三个会给出完全错误结论的坑 `实测`

E07/E08 过程中各踩一次，每个都会让结论反向。

### ① 列错位 → 假 NO-GO（0.037）

`pandas.read_csv(usecols=...)` 按**文件列序**返回，不是传入顺序。四个文件列序不同
（K562 从 `NAF1` 起、RPE1 从 `MRPS31` 起），不重排就是拿 A 的第 $j$ 个扰动和 B 的
**另一个**扰动比。

识破的依据不是「看起来太低」，而是算了随机期望：Jurkat(22 个显著) vs HepG2(25 个)
交集 $\approx 0.1$ 个基因，而 $25\times25/6642 = 0.094$ —— **测出来恰好等于随机水平**，
这是列错位的指纹。修法：`read_matrix` 末尾强制 `df[cols]` + 真实文件回归测试
（先断言四个文件原始列序确实不同，否则测试本身失效）。

### ② `K/G` 是错的基线 → 虚高到 1.05

真实数据里有一批**常常上榜**的基因（核糖体、翻译应激、细胞周期）在每个细胞系的
多数扰动 top-K 里都出现，制造出与扰动特异性迁移无关的跨系重叠。改用**打乱扰动标签
的置换零假设**（随机非零循环移位，无不动点，保留基因边缘上榜频率与 top-K 规模）后，
置换基线占原始 $h_{\text{cross}}$ 的**约 2/3**，比值从 1.050 降到 0.760。

### ③ 不做反卷积就数显著 → 噪声底 981

[E08](../experiments/E08-target-vs-essential/) 第一版直接拿 K562 的 $\hat\beta$
与 context 的 MDE 比大小，得 $\lvert R_p\rvert = 981$。**那是噪声底**：$\hat\beta$ 的
噪声 SD = `lfcSE` $\approx 0.1987$，真值 $\beta=0$ 的基因也有
$2\Phi(-0.30/0.1987) = 13\%$ 越阈，乘 8,248 个基因 $\approx$ **1,080**。
把带噪估计当真值过阈值必须先建模测量误差。

> **另有一个测试自身的坑**：fission 的独立性**不是代数恒等式而是分布陈述**
> （$\mathrm{Cov} = \mathrm{Var}(\hat\beta) - \sigma^2$）。测试里误传 `np.zeros` 当
> $\hat\beta$ 时相关跑出 $-1.0$。已单独钉成回归测试。

---

## F18 · 本届靶基因被刻意选成非必需，且在 K562 里几乎不做事 `实测`

### 与必需基因面板零重叠

| 检查 | 结果 |
|---|---|
| 本届 300 个靶基因落在 Nadig 必需面板（2,395 基因）内 | **0 / 300** |
| 随机期望（面板占蛋白编码基因组 12.0%） | 36 个 |
| 二项检验 $H_0$ = 随机抽样 | **$p = 2.03\times10^{-17}$** |

命名体系已核对（两边 HGNC symbol），面板真实性已核对（20/20 经典必需基因
RPL5/POLR2A/PCNA/RAN/SF3B1/VCP… 全在内）。**主办方刻意排除必需基因** ——
必需基因敲低导致生长停滞或死亡，拿不到每扰动 400 个健康细胞。

### 同细胞系内的严格对照

`K562GenomeWide` 一个文件里同时含必需与非必需扰动，**同库同管线，无批次混淆**：

| 组 | n | $\lvert R_p\rvert$ 中位 | fission $h_{\text{top288}}$ | $\pi_0$ | $\tau$ | 形状 |
|---|---|---|---|---|---|---|
| E 必需 | 2,053 | **123** | 0.139 | 0.4749 | 0.216 | 广而浅 |
| **V 本届靶** | **272** | **15** | **0.080** | 0.9946 | 0.495 | **稀而深** |
| O 其他非必需 | 1,200 | 11 | 0.073 | — | — | — |

8.2 倍差异是**纯生物学**。折扣系数（top-K 功效匹配口径）0.53–0.71。

**唯一对我们有利的一项**：本届靶基因是稀疏大效应（$\pi_0=0.995$、$\tau=0.50$），
离阈值更远、跨系更稳，**比必需基因的弥散小效应好预测**。

### 通用轴该从非必需扰动估

枢纽度向量相关：E vs V 的 top-288 Jaccard = 0.613，而 **V vs O = 0.772**。
→ $u_c$ 改从 K562GW 的 7,541 个非必需扰动估，弃用必需面板。

---

## F19 · 两个 $\mathbb{E}\lvert R_p\rvert$ 都对，差的是**测序深度** `推算`

> **更正记录（2026-08-28）**：本条先前把差异归因于「靶基因选择偏差 6.5×」。**否证了。**
> Arc 明确说明面板选法是先做 pilot screen、**按 DE 基因数分箱后跨
> negligible/subtle/strong 全谱采样**，刻意包含弱响应扰动 —— 所以主办方的靶基因面板
> **是无偏抽样，不是「响应者集合」**。原文保留在 git 历史里。

| 来源 | 值 | 量的是什么 |
|---|---|---|
| [F8](#f8) 官方 `jac` 基线反推 | **288** (207–365) | 本届 context 里真实的 $\lvert R_p\rvert$ |
| E08 经验贝叶斯（K562 效应量 + 本届真实 MDE 曲线） | **11** | 同一批靶基因**在 K562 GWPS 里能看见的** $\lvert R_p\rvert$ |

**F8 站得住**：$288/9863 = 0.0292$ 落在官方 $0.021$–$0.037$ 内，而
$11/9863 = 0.0011$ 低 20 倍；前提「官方基线是退化提交」有 `mse` 基线
$\approx 0.986$–$0.992$ 独立支持。

### 真正的机制：总测序深度差 21 倍

| 数据集 | cells/扰动 | UMI/cell | 总 UMI/扰动 |
|---|---|---|---|
| 2025 H1（与本届**同协议**） | 1,090 | 54,447 | **59.35 M** |
| 本届 2026 | 400 | 20,000 | 8.00 M |
| K562 GWPS | ~250 | ~1,500 | **0.38 M** |

$$\frac{\text{本届}}{\text{K562}} = 21.3\times \quad\Longrightarrow\quad z\ \text{膨胀}\ \sqrt{21.3} = 4.62\times$$

经验贝叶斯管线本身有三重交叉验证（复现独立 BH 计数 16 vs 15、136 vs 123；
推出的可达 $\mathbb{E}[J]=0.577$ 略高于官方锚点 $J=0.399$；三个 context 彼此一致），
**所以问题不在管线，在源数据的深度**。

---

## F20 · ~~$\mathbb{E}\lvert R_p\rvert$ 很可能是几十而不是 288~~ **已被 [F23](#f23) 推翻** `作废`

> **第三次更正（2026-08-30）**：[E10](../experiments/E10-h1-erp/) 在**主办方自己的
> 2025 H1 数据**上、用**官方 DE 机器**、降采样到**本届确切条件**实测得
> $\lvert R_p\rvert$ 中位 **253** —— [F8](#f8) 的 288 成立，本条的「几十」结论错误。
>
> **错在哪**：本条依据的三个外部观测（K562 EB 11 · K562 MASH 7 · CD4+T Rest MASH 36）
> 用的是 **DESeq2 / pseudobulk + donor 回归**，而官方打分用的是
> **Wilcoxon on cells + 每扰动 BH**。两者不是同一个量，差 26 倍来自
> **伪重复**（把 400 个细胞当独立重复），不是生物学也不是深度。详见 [F23](#f23)。
>
> 下面保留原文，因为其中三段仍然有效且被 F23 复用：
> ① $\tau/\text{se}$ 守门指标的数值标定；② $(\pi_0,\tau)$ 沿脊弱可辨识；
> ③ F8 推导确实欠定（288 只是解族之一）—— 只是 E10 独立地把解定在了 253。

> **第二次更正记录（2026-08-28）**：本条先前写「K562 的 $\pi_0$ 被低深度污染而偏高，
> 所以 44 是低估」。**方向错了。** 数值实验证明低深度让 $\mathbb{E}\lvert R_p\rvert$
> **先高估、再崩到 0**，不是低估；而 K562 恰好落在良态区。更早还写过
> 「单源 $h_{\max}=0.153$ 硬界」，也已撤回。两次都保留在 git 历史里。

### 反卷积的可用性由 $\tau/\text{se}$ 守门（已数值标定）

实测（真值 322 个基因越阈 0.30，$\tau = 0.5$，20,000 个基因）：

| $\tau/\text{se}$ | 拟合 $\pi_0$ | $\mathbb{E}\lvert R_p\rvert$ | 真值 | 判定 |
|---|---|---|---|---|
| 2.78 | 0.970 | **315.5** | 322 | 良态，准确 |
| 1.39 | 0.966 | 331.3 | 322 | 良态 |
| 0.93 | 0.897 | **648.3** | 322 | 开始高估 |
| 0.56 | 0.900 | **984.0** | 322 | 严重高估 |
| 0.35 | 0.900 | **0.0** | 322 | 彻底崩溃 |

机制：$\tau/\text{se}$ 变小时两成分方差趋同（$N(0,\text{se}^2)$ vs
$N(0,\text{se}^2{+}\tau^2)$），$(\pi_0,\tau)$ 沿脊不可辨识，估计可往任何方向跑。
**低深度源数据的危险是高估，不是漏检。**

$$\text{K562 GWPS: } \tau/\text{se} = 0.495/0.199 = \mathbf{2.49} \;\Rightarrow\; \textbf{良态}$$

**所以 E08c 给 K562 的 $\mathbb{E}\lvert R_p\rvert \approx 11$ 是可信的。**
守门指标已实现为 `vcclab.callset.identifiability`，随每次拟合强制报出。

### $(\pi_0,\tau)$ 沿脊互换 —— 别单报参数

$\pi_0 = 0.99$、40k 基因、se = 0.20 时的剖面似然：

| $\tau$ | 剖面 $\pi_0$ | 非零个数 | $-\log L$ |
|---|---|---|---|
| 0.50 | 0.9884 | 464 | −43332.40 |
| 0.55 | 0.9902 | 391 | −43331.73 |
| 0.62 | 0.9920 | 320 | −43328.51 |

整段只差约 4 个 log 单位。**单个参数弱可辨识，但下游的 `p_exceed` 在脊上是稳的**
（合成数据上 $\mathbb{E}\lvert R_p\rvert$ 回收误差 <15%）。测试相应地：参数用宽容差、
预测量用紧容差。

### F8 的推导欠定：288 只是解族里的一个

F8 用 $b_{\text{jac}} \approx \mathbb{E}\lvert R_p\rvert / 9{,}863$ 反推，
**前提是官方基线报满 9,863 个基因**。但 [F7](#f7) 测的是**我自己构造的**退化提交，
不是官方基线。把基线报数 $K_b$ 当未知量重算 $\text{jac} = m/(n + K_b - m)$：

| $\lvert R_p\rvert$ | $K_b{=}9863$（退化） | $K_b{=}2000$ | $K_b{=}500$ | $K_b{=}100$ |
|---|---|---|---|---|
| 11 | 0.0011 | 0.0055 | **0.0220** ✓ | 0.1100 |
| 30 | 0.0030 | 0.0150 | 0.0600 | 0.3000 |
| 60 | 0.0061 | **0.0301** ✓ | 0.1200 | 0.6000 |
| 288 | **0.0292** ✓ | 0.1440 | 0.5760 | 0.3472 |

（✓ = 落在官方 $b_{\text{jac}} = 0.021$–$0.037$ 内）

**$\lvert R_p\rvert = 11$、30、60、288 都能配出官方区间**，取决于基线报了多少个基因。
F8 的 288 依赖一个未经证实的前提。

### 三个直接观测都指向几十

| 来源 | $\lvert R_p\rvert$ | 功效 | 可信度 |
|---|---|---|---|
| K562 GWPS 经验贝叶斯 | **11** | $\tau/\text{se}=2.49$ 良态 | 高 |
| K562 MASH（外部，[F22](#f22)） | **7** | 作者功效校正 | 高 |
| **CD4+T Rest MASH（外部，[F22](#f22)）** | **36**（p75 95） | 512 细胞/扰动、Flex，**与本届可比** | 高 |
| F8 反推 | 288 | — | **欠定** |

$$\boxed{\ \mathbb{E}\lvert R_p\rvert \approx 10\text{–}95\ \text{（点估计约 36）},\ \textbf{不是 288}\ }$$

### 后果：$K_p$ 这个最大杠杆之前错了约 10 倍

$K_p$ 应定在**几十**量级。若真值 $n = 36$ 而报 $K = 288$，
Jaccard 上限只有 $36/288 = 0.125$（即便全部命中）；报 $K = 36$ 且全中则为 1.0。

### 决定性验证（一次线上提交即可）

令 $\hat R$ = 全部门内基因，回读 `jac` 原始分 $= \lvert R_p\rvert / 9{,}929$。
这直接测出 $\mathbb{E}\lvert R_p\rvert$，把整条推算链变成事实。
**在花任何算力做 Stage 1 之前先做这个。**

---

## F21 · 最优召集集合的两个命题（已证明 + 数值验证） `理论确认`

### 命题 1 — 固定 $K$ 下按 $p_g$ 取前 $K$ 个是最优

$\mathbb{E}|R_p \cap \hat R_p| = \sum_{g\in\hat R_p} P(g\in R_p)$ 对成员指示**线性**，
故取最大的 $K$ 个。数值核对：$\Sigma p = 132.81$（前 288）vs 随机 31.25、倒序 0.02；
2000 次随机集合里最好的仍差 93.07。

> 严格地说 $h$ 的分母 $|R|$ 也是随机的，但它完全不依赖 $\hat R$，
> **条件在扰动强度上（给定 $|R_p|=n$）时上式精确最优** → 所以要按扰动分别定 $K_p$。

### 命题 2 — 最优 $K$ 的停止规则

$$\frac{d\,\mathbb{E}[J]}{dK}=0 \iff p_{K^*} = \frac{M(K^*)}{n+K^*} = \frac{J}{1+J} = \frac{h}{2}$$

**只要下一个基因的检出概率高于当前已达 $h$ 的一半，就继续加。**
官方锚点核对：$J=0.399 \Rightarrow h=0.570 \Rightarrow p^*=0.285=h/2$ ✓

数值核对（暴力搜索）：指数衰减 0.9% · 平缓 beta 1.4% · 陡降 beta 4.6% 误差；
**阶跃型 68.4% 失效**——推导假设 $p$ 对 $K$ 可微，阶跃时最优点落在悬崖上。

### 由失效模式推出的设计决定

阶跃型 $p$ 对应「**已知** $R_p$」，平滑型对应「**预测不确定**」。我们的 $p_g$ 是 probit
出来的连续量，必然平滑，所以 $K^*_{\text{我们的}} > \mathbb{E}|R_p|$：
**不确定性越大，该召集的基因越多**（Jaccard 分母只按加进去的假阳性增长，
而每个 $p > J/(1+J)$ 的基因仍净赚）。实现里不用闭式，直接按扰动数值最大化
$\mathbb{E}[J]$，自动覆盖两种形状。

验证代码：[`experiments/E09-framework-theory/verify.py`](../experiments/E09-framework-theory/verify.py)

---

## F22 · 外部独立验证：跨系可迁移性 = 同系复现的 0.708，且是在本届真实靶基因上测的 `实测（外部）`

来源：Zhu/Marson 等，*Cell* 2026（PMID 42664972）随论文发布的
`K562_comparison.suppl_table.csv`（969 KB，可直接 curl）。
作者用 **MASH 做过功效校正**，比较 K562 与 primary CD4+T（**Rest = 无刺激臂**）
的 3,081 个共享扰动。

### 这比 E07 更可信，因为它测的是本届的真实靶基因

| 量 | 全体（两侧都有 MASH DEG，n=1,880） | **本届 87 个重叠靶基因** |
|---|---|---|
| 跨系 LFC Pearson $r$ | 0.313（中位 0.303） | **0.339**（中位 0.330） |
| 随机对照 $r$ | 0.006 | — |
| donor 内（同系）复现 $r$ | 0.441 | 0.479 |
| **跨系 / 同系比值** | **0.710** | **0.708** |

跨系信号是随机对照的 **55×**，真实无疑。而 0.708 这个比值：

| 测量 | 比值 | 测在什么基因上 |
|---|---|---|
| [E07](#f16) 扣置换基线 | 0.430 | 必需基因（代理） |
| [E07](#f16) 原始 | 0.625 | 必需基因（代理） |
| **本条（外部）** | **0.708** | **本届真实靶基因** |

$$h \approx 0.708 \times 0.570 = \mathbf{0.404} \quad\text{vs 追平线 } 0.134 \quad\Longrightarrow\quad \text{余量 } \mathbf{3.0\times}$$

**E07 的必需基因代理是偏保守的**，真实靶基因上的可迁移性更好 —— 与 [F18](#f18)
「稀疏大效应比弥散小效应好预测」一致。

### 同时给出第三个 $\lvert R_p\rvert$ 观测（MASH 功效校正后）

| 数据集 | 全体中位 | 本届 87 个靶基因 | p75 |
|---|---|---|---|
| K562 | 7 | 7 | 16 |
| **CD4+T Rest** | 21 | **36** | 95 |

CD4+T（512 细胞/扰动、10x Flex，功效与本届可比）给出 36，是 K562 的 5 倍
—— 功效效应清晰。但**仍远低于 [F8](#f8) 反推的 288**。

> ⚠️ **$\mathbb{E}\lvert R_p\rvert$ 仍是未决问题，而它是最大的杠杆（决定 $K_p$）。**
> 三个观测 7 / 36 / 288 跨了 40 倍。[F19](#f19) 的深度解释**不足以解释 CD4+T 的 36**，
> 因为 CD4+T 功效与本届相当。可能性：(a) F8 的「官方基线是退化提交」前提有误；
> (b) 本届 context 对这批靶基因确实更敏感；(c) 官方 `jac` 是缩放分而非原始 Jaccard。
> **决定性验证有两条**：① 用 2025 H1 数据（主办方自己的协议与选基因流程）直接实测；
> ② 花一次线上提交，令 $\hat R$ = 全部门内基因，回读 `jac` 原始分 $=\lvert R_p\rvert/9{,}929$。

### 顺带：CD4+T Rest 是现成的第二源

| 属性 | 值 |
|---|---|
| 与本届 300 靶基因重叠 | **283 / 300 = 94.3%**（Rest 臂） |
| 每扰动细胞数 | 中位 **512**（本届 400） |
| 化学 | 10x GEM-X Flex（与本届同族） |
| 扰动系统 | genome-scale **CRISPRi**，Rest 臂**无刺激** |
| 现成产物 | `GWCD4i.DE_stats.h5ad` 的 `.layers` 直接给 `log_fc` / `lfcSE` / `p_value` / `adj_p_value` / `zscore` |
| 许可证 | MIT |

**不需要自建 pseudobulk 流水线。** 这是目前性价比最高的数据获取动作。

> **本机可达性**：`raw.githubusercontent.com` 可用（两个 suppl table 已下载）。
> 但 **HuggingFace 与 s3-eu-west-1 被企业代理拦截**，所以
> X-Atlas/Orion（HCT116 + HEK293T，≈300/300 覆盖，全基因组无刺激 CRISPRi）
> 的 parquet 分片与 figshare 直链**都不可达** —— 这是一个环境约束，不是数据问题。

---

## F23 · $\lvert R_p\rvert$ 实测：中位 **253**，但跨 350 倍 `实测`

[E10](../experiments/E10-h1-erp/)。这是本项目里唯一一次在**主办方自己的数据**上、
用**官方 DE 机器**、在**本届确切条件**下直接测这个数。它终结了 F8 / F19 / F20 之争。

### 为什么只有 2025 H1 能回答

同一批主办方 · 同一套 pilot-screen 分层选基因流程 · 同一套实验协议
（dual-guide CRISPRi + 10x Flex）· 基因面板几乎相同（18,080 vs 18,533，共享 18,076）·
深度是本届 7.4 倍**因而可以降采样到本届功效**。

### 降采样到本届的确切条件

| 量 | 本届 | H1 原始 | 本实验 |
|---|---|---|---|
| 对照细胞 | 18,400 | 38,176 | 抽 18,400 |
| 每扰动细胞 | 400 | 中位 1,090 | 抽 400（6 个不足者取全部） |
| UMI/细胞 | 20,000 | 54,447 | 二项稀释到 20,000（实测中位正好 20,000） |

### 结果

| 条件 | gate | p5 | p25 | **p50** | p75 | p95 | 均值 |
|---|---|---|---|---|---|---|---|
| **降采样到本届** | 10,779 | 2 | 7 | **253** | 2,418 | 4,703 | 1,226 |
| H1 原生功效 | 10,780 | 7 | 258 | 1,940 | 5,569 | 7,481 | — |

$$\boxed{\ \lvert R_p\rvert \text{ 中位 } = 253 \;\Rightarrow\; \textbf{F8 的 288 成立}\ }$$

50 个扰动里 **16 个 $\lvert R_p\rvert < 50$**（主办方刻意纳入的 negligible 箱）、
**17 个 $> 1000$**，只有 **6 个**落在 F8 的 [207, 365] 内。

### 26 倍的差异来自 DE 方法，不是生物学也不是深度

| 方法 | $\lvert R_p\rvert$ | 统计结构 |
|---|---|---|
| **官方打分**：Wilcoxon on cells + 每扰动 BH | **253** | 400 vs 18,400 个**细胞**当独立重复 |
| K562 / CD4+T 发表值：DESeq2 / pseudobulk + donor 回归 | 7–36 | 正确处理**生物学**重复 |

单细胞 Wilcoxon 把细胞当独立重复 = **伪重复**，大幅放大显著性。直接量到了指纹：

$$\text{细胞数 } 400 \to 1{,}090 \;\Longrightarrow\; \lvert R_p\rvert \text{ 中位放大 } \mathbf{4.0\times}$$

正确指定的检验，发现数不会随样本量这样陡增。

> **$\lvert R_p\rvert$ 由官方打分方法定义，不由生物学定义。**
> 外部数据集用 DESeq2/pseudobulk 算的 DE 计数是**另一个量**，绝不能代入。
> 我在 F19 和 F20 里各错一次，两次都是因为拿不同 DE 方法的量互相比较。

### 对框架的两个后果

**① 全局固定 $K$ 是严重错误，$K_p$ 必须按扰动定。**
对 $\lvert R_p\rvert = 2$ 的扰动报 288 个，Jaccard $\le 2/288 = 0.007$；
对 $\lvert R_p\rvert = 5{,}398$ 的报 288 个，Jaccard $\le 288/5398 = 0.053$。**两个尾巴都被砍。**
这让 [F21](#f21) 命题 1 里「条件在扰动强度上」从技术细节升级为**必要条件**。

**② 预测每扰动的响应幅度 $a_p$ 与预测哪些基因响应同等重要。**
$\lvert R_p\rvert$ 跨 350 倍而 $K_p$ 由它决定；$K$ 选错一个数量级的代价
大于基因排序错一半。**幅度不是二等公民。**

`vcclab.callset.best_k` 默认用 $\sum_g p_g$ 作 $n$，天然按扰动、不依赖全局先验
—— 这个设计正确，无需改动。

---

## F24 · 最承重的锚点由直接测量证实：$h_{\text{replicate}} = 0.550$ `实测`

[E11](../experiments/E11-replicate-anchor/)。追平榜首所需的 $h$ 此前是三层反推：

$$r_{\text{jac}} = 0.399 \xrightarrow{\;\mathrm{jac}=h/(2-h)\;} h_{\text{replicate}} = 0.570 \longrightarrow 0.23\times0.570 = 0.134$$

**整条链条上没有一个数是直接测的**，而所有 go/no-go 判断都挂在它身上。
现在用主办方自己的数据 + 官方 DE 机器，字面复现「真实数据对半劈开」：
对照 38,176 → 两个互不相交的 18,400；每扰动细胞 → 两个互不相交的 400；
UMI 稀释到 20,000；两半各自用**自己的**对照算 $R_p$。35 个扰动合格。

| 量 | 官方反推 | **实测中位** | IQR | 比值 |
|---|---|---|---|---|
| $h_{\text{replicate}}$ | 0.570 | **0.550** | [0.459, 0.692] | **0.96×** |
| $r_{\text{jac}}$ | 0.399 | **0.379** | [0.298, 0.530] | **0.95×** |
| 追平线 $h$ | 0.134 | **0.127** | — | 0.95× |

恒等式 $\mathrm{jac} = h/(2-h)$ 精确成立（$\lvert A\rvert/\lvert B\rvert$ 中位 = 1.000）。

### $h_{\text{replicate}}$ 不随 $\lvert R_p\rvert$ 单调变化（我的假设被否证）

| $\lvert R_p\rvert$ | n | $h$ 中位 |
|---|---|---|
| < 20 | 15 | 0.667 |
| 20–200 | 7 | **0.432** |
| 200–2000 | 8 | 0.475 |
| > 2000 | 5 | 0.682 |

U 形，Spearman $\rho = -0.262$、$p = 0.13$ **不显著**。两端是伪效应：
极小集合里 3/3 重叠就给 $h=1.0$；极大集合里多数基因强显著故两半必然一致。

**稳健性**：只看 $\lvert R_p\rvert \ge 200$ 的 13 个扰动（承载绝大部分可得分空间，
$\lvert R_p\rvert$ 中位 1,134）得 $h = 0.545$，与全体中位 0.550 几乎相同。
**锚点对该分层稳健，无需修正。**

### 整条链条现在全是实测值

| 环节 | 值 | 来源 |
|---|---|---|
| $h_{\text{replicate}}$ | **0.550** | E11 实测 |
| 追平榜首所需 $h$ | **0.127** | $= 0.23 \times 0.550$ |
| 跨系可迁移性比值 | **0.708** | [F22](#f22) 外部 MASH 校正，**本届真实靶基因** |
| 折算可达 $h$ | **0.389** | $= 0.708 \times 0.550$ |
| $\lvert R_p\rvert$ | **中位 253**，IQR [7, 2418] | [F23](#f23) 实测 |
| **余量** | **3.08×** | |

$\lvert R_p\rvert \ge 200$ 的子集给出同样余量（0.386 / 0.125 = 3.08×）。

---

## 附：本项目所有更正的通用化 → skill

本文件里的每条更正记录都被抽成了可复用的失效模式，写入
[`skills/auditing-quantitative-claims/`](../skills/auditing-quantitative-claims/)
（同时装在 `~/.claude/skills/`）。

18 条陷阱，按腐蚀对象分组：**有效性**（泄漏、过程不匹配、聚合不匹配、机制归因）·
**推断**（不做反卷积就过阈值、均匀零假设、恰好落在零假设上、选择污染、有效样本量）·
**解读**（Simpson 反转、相关当集合重叠、恒等式越界、赢过地板基线、检验与断言不匹配）·
**决策**（不对称损失下只优化精度）· **方法**（分布陈述当代数恒等式）·
**基础设施**（瞬时失败当能力缺失、脆弱的产物溯源）。

### 元模式

四个代价最高的错误里有三个（泄漏 50%、过程不匹配 26×、聚合不匹配 4×）是同一个形状：

> **拿一个量去和另一个不是同一个量的量比较。**
> 不同的域（泄漏）、不同的过程（26×）、不同的聚合（4×）。

**任何比较之前，把两边各自literally是什么写下来**——过程、总体、聚合方式。
两句话里任何一个从句不同，比较就无效，直到那个从句被修好。

---

## F25 · 六个计分指标的首次完整测量：`avg_score = 0.1110`，领先者 0.1899 `实测`

在此之前**全部约 20 个实验只优化了 `sig_jaccard`**。读 `cell_eval2` 0.16.0 源码后
才知道 `vcc2026` 有 6 个 `scoring.scored=True` 的指标，
总分 = 6 个 `from_baseline` 的**平均**（`aggregate_metrics` 的 docstring 明写
"one NaN-skipping value per metric over perturbations — the mean"）。

E27（8 个扰动，H1 为目标，官方 `compute_metrics` + `score_metrics`）：

| 指标 | 我们 | 退化基线 | `from_baseline` |
|---|---|---|---|
| `expr_mse_unbiased_capped_norm` | 1.2422 | 2.5985 | **+0.5219** |
| `de_wilcoxon_direction_reach_raw` | 0.1348 | 0.0073 | **+0.1284** |
| `pds_cosine` | 0.5179 | **0.500000** | **+0.0357** |
| `de_wilcoxon_sig_jaccard` | 0.0485 | 0.0475 | **+0.0010** |
| `de_wilcoxon_direction_fidelity_yield_raw` | 0.4954 | 0.4962 | **−0.0017** |
| `de_wilcoxon_lfc_nmae` | 1.0168 | 0.9977 | **−0.0191** |
| **`avg_score`** | | | **0.111049** |

三条硬结论：

1. **78% 的分数不是生物学。** `expr_mse_unbiased_capped_norm` 独占 0.522/0.666，
   而它之所以高，是因为退化基线（对照均值复制 400 遍）**组内方差为零**被无偏 MSE 重罚。
   我们只是有真实的离散度。
2. **`sig_jaccard` 的 `from_baseline` = 0.000993。** 约 20 个实验换来六分之一指标上的
   0.001，即 `avg_score` 的 **0.00017**。这是"优化错东西"的实测代价。
3. **两个指标为负** —— `lfc_nmae` 和 `direction_fidelity` 比直接贴对照均值更差。

`pds_cosine` 的退化基线**精确等于 0.500000**，与 `metrics/discrimination.py` 注释里
"Under `panel` those arms measure exactly 0.5000"逐位一致 —— 源码的实测锚点可信。

## F26 · `hamilton()` 的确定性取整摧毁 `pds_cosine`，纯实现修复值 +0.393 `实测`

`pds_cosine` 是**排名**指标：$\text{PDS}_p = 1 - \text{rank}(\text{self})/D$，$D = n-1$，
只要求 `pred_eff[p]` 离 `real_eff[p]` 比离 `real_eff[j≠p]` 更近 ——
**只需相对可区分性，不需要绝对精度**。它只吃 pseudobulk 均值，不需要 DE，
所以可本地精确重算：E28 的 harness 复现官方值到 $1.1\times10^{-16}$，**12.65 s vs 官方 2,643 s（209×）**。

损失分解（同一设计，真实细胞，官方 pds 函数）：

| 阶段 | pds | `from_baseline` |
|---|---|---|
| 闭式（无抽样） | 0.7321 | +0.4643 |
| 泊松 400×1e6 | 0.7321 | +0.4643 |
| 泊松 400×19.5k | 0.6607 | +0.3214 |
| **真实提交（hamilton + 两点构造）** | **0.5179** | **+0.0357** |
| 同一设计换多项式抽样，1e6 | 0.7143 | **+0.4286** |

抽样噪声只要 0.071 原始 pds；**解码器自己吃掉 0.214，是抽样的 3 倍**。

根因：`hamilton()` 是确定性最大余额法。18,080 个基因里 CPM ≈ 0.016 的基因余额排在末尾，
**在每一个细胞都分到 0 个计数**，故池计数恒为 0 —— 2,264 个基因被确定性清零，
在每一行产生**相同的** $-\log_1p(\text{bts}\cdot c)$ 伪影。共享分量正是把所有余弦拉到一起、
摧毁排名的东西。真实数据里的 2,277 个零基因是真抽样产生的，**不是同一批基因，无法抵消**。

## F27 · 两点 $\bar\psi$ 构造的实测交换比：买 0.001，卖 0.43 `实测`

两点构造存在的唯一理由是为 `sig_jaccard` 精确控制 $\bar\psi$，而它需要确定性取整。
实测代价：

$$\underbrace{+0.001}_{\text{sig\_jaccard 的全部收益}} \quad\text{vs}\quad \underbrace{-0.43}_{\text{pds\_cosine 的 from\_baseline}}$$

**倍数 430×。** 结论：放弃两点构造，用多项式抽样。显著性交给均值偏移自然产生 ——
真实数据本来就是这么来的。

这也是 F-系列里第二次出现同一个形状的错误（见 F19/F20 的更正）：
**为了精确控制一个便宜的量，付出了一个贵的量的代价，而两者的价格从未被同时测量过。**

## F28 · ~~`lfc_nmae` 的真实障碍是源侧基因覆盖 41%~~ **已被 [F33](#f33) 从根上推翻** `作废`

E27 诊断出 `lfc_nmae = 1.0168` 的直接原因是只给召集集合（29–288 个基因）赋了 lfc，
其余 lfc = 0，而分子在 $\text{lfc}_{\text{pred}}=0$ 时恰等于分母。

但把 lfc 铺到全部基因后，本地估计的 nmae **在 8 种设计下全部精确等于 1.0000**。
根因不是铺没铺开：**K562GW 只覆盖 7,481/18,080 个基因（41%）**，
而真实侧显著集大量落在剩下的 59% 上，那里我们只能预测 0。

所以 `lfc_nmae` 的上限由**源侧基因覆盖**决定，不由算法决定：
即使在覆盖到的 41% 上做到完美，nmae 也只能降到约 0.59。
这把"补齐基因覆盖"（CD4+T 源、多源并集）从可选项变成了该指标的**唯一**路径。

### 更正记录 · F26 与 F27 都不完整（2026-09-21）

**F26 的错误**：我断言 `hamilton()` 的确定性"只有害处"。它有两面，我只测了一面。

确定性同时意味着**零抽样噪声**。换成纯多项式抽样后，召集集合小的三个扰动
（K=29：TCF7L2、VCL、ZNF581）的 pds 从 `0.143 / 0.286 / 0.714` 掉到 `0.0 / 0.143 / 0.0`
—— 29 个基因的信号被 20k 深度的多项式噪声淹没。hamilton 的无噪声特性在 K 小时是关键优势。

**F27 的错误**：我断言两点 $\bar\psi$ 构造"买 `sig_jaccard` 的 0.001，卖 `pds` 的 0.43"。
它买的**远不止** 0.001。V2 实测丢掉它之后：

| 指标 | 变化 |
|---|---|
| `de_wilcoxon_direction_reach_raw` | **−0.1278** |
| `expr_mse_unbiased_capped_norm` | **−0.5219** |
| `de_wilcoxon_sig_jaccard` | −0.0010 |

我在只测了一个指标的情况下宣布了一个跨六指标的交换比。**倍数 430 是凭空来的。**

## F29 · 连续三次归因失败：一次只改一个东西 `方法`

| 变体 | 我以为改了 | 实际改了 | `avg_score` |
|---|---|---|---|
| A（基线） | — | — | **0.1110** |
| V2 | 量化器 | 量化器 + top1000 + 去面板均值 | 0.0922 |
| V3 | **只有**量化器 | 量化器 + 排序统计量（$\lvert\beta\rvert$ vs $\lvert\beta\rvert/\text{SE}$）+ K（重算的 $n_{\text{src}}$ 不同：MAT2A 154 vs 82 → K 7481 vs 288）+ 生成模型（丢掉 `bootstrap`，改纯多项式） | **−0.0119** |
| V4 | 只有量化器 | 由 E27 的 build **逐字复制**，仅 `quantizer="stochastic"` | 见下 |

V3 是最贵的一次：我在提交信息里写了"单旋钮实验"，而它同时动了四件事。
**检测信号**：如果一个变体不是由基线代码逐字复制 + 一处改动生成的，它就不是单旋钮实验，
不管意图如何。V4 是用 `Path(...).read_text()` + 两次 `str.replace` + `assert` 命中
生成的，这才是可归因的做法。

`de_direction_reach` 的定义解释了为什么这些"顺手"的改动这么贵：它测的是
**我们自己 $\lvert\text{lfc}\rvert$ 排序的方向纯净前缀深度** $k^*(t)/N_{\text{conf}}(t)$。
任何改变排序的操作（去面板均值、换排序统计量）都会让纯前缀立刻崩掉，
而这一点在 `pds_cosine` 上是**收益**。三个指标的要求互相冲突：

| 指标 | 要求 | 去面板均值 |
|---|---|---|
| `pds_cosine` | 可区分性（相对） | **帮** |
| `de_direction_reach` | 排序顶部符号纯净 | **毁** |
| `expr_mse_unbiased` | 绝对精度 | **毁** |

### 一个被验证的工具（正面结果）

`pds_cosine` 的本地 harness 被**独立验证两次**：
在 variant A 上复现官方值到 $1.1\times10^{-16}$，在 V3 上复现官方的 `0.500000`（精确）。
12.65 s vs 官方 2,643 s。它只需 pseudobulk 均值，不碰 DE。
**结论：pds 的设计搜索可以本地做，其余五个指标不行** —— V3 的教训是
harness 忠实不代表 build 忠实，两者必须分别验证。

## F30 · 三步单旋钮把 `avg_score` 从 0.1110 推到 0.1873（领先者 0.1899 的 98.6%）`实测`

每一步都由上一步的 build **逐字复制 + 一处 `str.replace` + `assert` 命中**生成，
所以每一行变化都可归因（这是 F29 的教训换来的纪律）。

| 变体 | 改动 | `avg_score` | Δ |
|---|---|---|---|
| A | 基线：$\lvert\beta\rvert/\text{SE}$ 排序，K=29/288/G，$\lambda$=1.0 | 0.1110 | — |
| **V5** | 排序 → $\lvert\beta\rvert$ | **0.1559** | +0.0449 |
| **V6** | $\lambda$ → 0.5 | **0.1762** | +0.0203 |
| **V7** | K → 扁平 288 | **0.1873** | +0.0111 |

被否证的三条（同样记录）：

| 变体 | 改动 | `avg_score` |
|---|---|---|
| V2 | 量化器 + top1000 + 去面板均值（三改一起） | 0.0922 |
| V3 | 自称单旋钮，实际四改 | −0.0119 |
| V4 | `hamilton` → `stochastic_round` | 0.1109（与 A 逐位相同）|

### 逐指标（V7 vs A）

| 指标 | A | V7 | 剩余空间 |
|---|---|---|---|
| `pds_cosine` | 0.0357 | **0.5000** | 0.500 |
| `expr_mse_unbiased_capped_norm` | 0.5219 | **0.5988** | 0.401 |
| `de_wilcoxon_lfc_nmae` | −0.0191 | **0.0092** | 0.991 |
| `de_wilcoxon_direction_reach_raw` | 0.1284 | 0.0147 | **0.985** |
| `de_wilcoxon_sig_jaccard` | 0.0010 | 0.0009 | 0.999 |
| `de_wilcoxon_direction_fidelity_yield_raw` | −0.0017 | 0.0000 | 1.000 |

### 三个机制（都是实测，不是推断）

1. **排序统计量是 `pds_cosine` 的主因**：$\lvert\beta\rvert$ 比 $\lvert\beta\rvert/\text{SE}$ 高
   +0.393 `from_baseline`。余弦由大坐标主导，$\lvert\beta\rvert$ 把信号放在余弦看得见的地方；
   $\lvert\beta\rvert/\text{SE}$ 选高置信度基因（常是高表达小效应），在余弦里几乎不可见。
   ⚠️ $\lvert\beta\rvert/\text{SE}$ 是 E18 为 `sig_jaccard` 选出的最优排序 ——
   **为 1/6 的指标调出来的最优，在另一个 1/6 上是最差。**

2. **余弦对尺度不变，所以 $\lambda$ 收缩对 `pds` 免费**：$\lambda$ = 1.0/0.7/0.5 的 pds
   全部是 0.7143。这一步的代价只可能落在别处 —— `lambda_probe.py` 先在本地证明了这点，
   再用官方打分器确认代价落在 `direction_reach` 上。

3. **$\lambda$ 上的直接冲突**：`direction_reach` 要大 lfc（实现值须高过 bootstrap 噪声，
   否则符号随机化、纯前缀立刻崩），`expr_mse`/`lfc_nmae` 要收缩 lfc（接近 MSE 最优）。
   $\lambda$: 1.0 → 0.5 使 reach 从 0.1424 掉到 0.0132，而 expr_mse 从 0.3930 涨到 0.6018、
   `lfc_nmae` **首次转正**。净 +0.0203，故 0.5 胜。线性外推下 V7 已是最优。

### ⚠️ 未解决的风险：官方打分器标记了我们的提交

V6/V7 都触发同一条警告：

> across-perturbation spread 0.003167 vs claimed sampling correction 0.006544
> (ratio 0.484 < 0.7)。**per-cell scatter 在 pseudobulk 里基本抵消，这不像一个
> i.i.d. 抽样的预测细胞群** —— 诚实的提交测出约 1.0，钉住聚合的那两条 arm 测出 0.006 和 0.000。
> 校正是有界的（issue #348）故分数未被抬高，但这份提交值得查看。

根因在 `design_cells`：`V *= tgt / np.maximum(V.mean(0), 1e-12)` **强制**经验均值精确
等于目标 profile，消除了均值自身的抽样波动。作为比赛提交这是**可检测的伪影**，
组织者明确在看。修法是让均值按 i.i.d. 抽样自然波动，代价未测。

### 更正记录 · F30 的「线性外推下 V7 已是最优」是错的（2026-09-21）

V8（$\lambda$ = 0.7）实测 **`avg_score` = 0.2025**，比 V7 的 0.1873 高 +0.0152。

`de_direction_reach` 对 $\lambda$ **饱和**，不是线性：

| $\lambda$ | `direction_reach` | `expr_mse_capped_norm` | 两者之和 | `avg_score` |
|---|---|---|---|---|
| 1.0 (V5) | 0.1424 | 0.3930 | 0.535 | 0.1559 |
| **0.7 (V8)** | **0.1419** | **0.5836** | **0.726** | **0.2025** |
| 0.5 (V6) | 0.0132 | 0.6018 | 0.615 | 0.1762 |

$\lambda$ 从 1.0 降到 0.7，reach 几乎不动（0.1424 → 0.1419，−0.4%）而 expr_mse 涨了
0.19 —— **收缩的前 30% 几乎是纯收益**。再降到 0.5，reach 就掉下悬崖（0.0132）。
机制符合 $k^*$ 的定义：它是纯度仍 ≥ $P_0$ 的最深处，只要实现 lfc 还高过 bootstrap 噪声，
$k^*$ 就由**符号纯度**上限而非幅度决定；一旦低于噪声，符号随机化，$k^*$ 塌缩。

我之所以做了错的外推：只有 $\lambda$ = 1.0 和 0.5 两个点，两点之间只能连直线。
**两个点不能判断一条曲线是凸的还是有悬崖的** —— 这条进 traps.md。

## F31 · ~~V8 是首个超过榜首数值的配置：`avg_score` = 0.2025~~ **水平结论已被 [F32](#f32) 推翻，排序结论部分保留** `已更正`

| 变体 | 改动 | `avg_score` |
|---|---|---|
| A | 基线 | 0.1110 |
| V5 | 排序 $\lvert\beta\rvert/\text{SE}\to\lvert\beta\rvert$ | 0.1559 |
| V6 | $\lambda\to 0.5$ | 0.1762 |
| V7 | $K\to 288$ | 0.1873 |
| **V8** | $\lambda\to 0.7$ | **0.2025** |

逐指标（V8 vs A）：

| 指标 | A | V8 | 剩余空间 |
|---|---|---|---|
| `pds_cosine` | 0.0357 | **0.5000** | 0.500 |
| `expr_mse_unbiased_capped_norm` | 0.5219 | **0.5836** | 0.416 |
| `de_wilcoxon_direction_reach_raw` | 0.1284 | **0.1419** | 0.858 |
| `de_wilcoxon_lfc_nmae` | −0.0191 | −0.0026 | 1.003 |
| `de_wilcoxon_direction_fidelity_yield_raw` | −0.0017 | −0.0087 | 1.009 |
| `de_wilcoxon_sig_jaccard` | 0.0010 | 0.0007 | 0.999 |

⚠️ **这个 0.2025 不能直接和榜首的 0.1899 比**，三处过程不匹配：
基线不同（我用自建的退化基线，官方用官方基线，`from_baseline` 的分母因此不同）、
8 个扰动 vs 300 个、1 个 context vs 3 个。
**内部有效的结论是：同一标尺下三步改动把分数抬了 82%**（0.1110 → 0.2025）。
跨标尺的"超过榜首"没有建立。

## F32 · 换到官方刻度后 V8 = **0.1215**，没有超过榜首 0.1899；`mse` 全程是 0 `实测`

F31 的 0.2025 的分母是**我们自建的退化基线**。把同一批 raw 值搬到唯一能和榜首比较的
刻度（官方 $b$ / $r$，[01-scoring.md:167-174](01-scoring.md) 从实时榜反算），算法不是手写
算术而是把 `cell_eval2` 的 policy `anchor` 换成实测 $r$ 后交回 `scoring.score_one`
（`anchor_local.py:158-180`，与 `score.py:431` / `score.py:270` 同一次调用）：

| 变体 | 改动 | 官方 $b_{lo}/r_{lo}$ | 官方 $b_{hi}/r_{hi}$ | 退化基线（F30/F31 用的） | frozen `v10` |
|---|---|---|---|---|---|
| A | 基线 | 0.0263 | −0.0005 | 0.1110 | −0.0082 |
| V2 | 量化器+top1000+去面板均值 | **0.1080** | 0.0677 | 0.0922 | −0.3032 |
| V3 | 四改 | −0.1085 | −0.1283 | −0.0119 | −0.1920 |
| V4 | `stochastic_round` | 0.0260 | −0.0007 | 0.1109 | −0.0084 |
| V5 | 排序 → $\lvert\beta\rvert$ | 0.1028 | 0.0675 | 0.1559 | 0.0023 |
| V6 | $\lambda$ → 0.5 | 0.0909 | 0.0552 | 0.1762 | 0.0784 |
| V7 | $K$ → 扁平 288 | 0.1041 | 0.0668 | 0.1873 | 0.0886 |
| **V8** | $\lambda$ → 0.7 | **0.1215** | **0.0848** | **0.2025** | **0.0996** |
| 榜首 `GeroAI_v11` | — | **0.1899** | 0.1899 | — | — |

**V8 在四把尺子上都是最好的变体，所以「三步单旋钮有效」成立。**
但 F31 的水平结论不成立：官方刻度下 V8 落在榜首的 **64%**（lo 端）到 **45%**（hi 端）。

### V8 vs 榜首，逐指标（官方 lo 端）

| 指标 | V8 raw | 榜首 raw | V8 scaled | 榜首 scaled | Δ | 折成 avg |
|---|---|---|---|---|---|---|
| `pds_cosine` | 0.7500 | 0.820 | 0.5855 | 0.7080 | −0.1225 | −0.0204 |
| `expr_mse_capped_norm` | 1.0819 | 0.959 | **0.0000** | 0.0410 | −0.0410 | −0.0068 |
| `sig_jaccard` | 0.0482 | 0.029 | **0.0769** | −0.0040 | **+0.0809** | **+0.0135** |
| `lfc_nmae` | 1.0004 | 0.892 | 0.0009 | 0.1780 | −0.1771 | −0.0295 |
| `direction_fidelity_yield` | 0.4919 | 0.514 | −0.0453 | 0.0030 | −0.0483 | −0.0081 |
| `direction_reach` | 0.1482 | 0.267 | 0.1110 | 0.2130 | −0.1020 | −0.0170 |
| **avg** | | | **0.1215** | **0.1899** | **−0.0683** | |

### 三条因此才看见的事实

**1. `expr_mse_capped_norm` 对全部 8 个变体都是 0.0000 —— 这一项我们根本没在得分。**
官方 $b$ = 0.986–0.992，我们最好的 raw 是 V6 的 1.0347，**全部差于官方基线**，
`clamp_low=0` 一律压到 0。旧尺子给 0.58 只因为退化基线的 raw 是 2.5985（差 2.6 倍）。

→ **[F30](#f30) 机制三（「$\lambda$ 收缩用 `expr_mse` 换 `direction_reach`」）在官方刻度下是虚的**：
$\lambda$ 取任何值 `mse` 都是 0，真实的 $\lambda$ 权衡只剩 `reach` vs `nmae`。
V8 胜 V7 的 +0.0174 逐项拆开：`reach` **+0.0231**（−0.0276 → 0.1110），
`nmae` −0.0031、`fid` −0.0025、`jac` −0.0001，`pds` 和 `mse` 逐位不动。
**买到的全部是 `reach`，付出的是 `nmae`/`fid`；mse 一分钱没参与。**
进入得分区的门槛是可量化的：raw 须 **< 0.986**，V6 的 1.0347 要再降 **4.7%**。

**2. `sig_jaccard` 是我们唯一超过榜首的指标：+0.0809。**
全场 315 队在这一项都是 0（[01-scoring.md:171](01-scoring.md) 榜首 −0.004），
我们的 raw 0.0482 高过官方 $b_{lo}$ 0.021。折 avg **+0.0135**。
这来自 [F21](#f21) 的最优召集集合命题，是目前唯一独有的优势 ——
**后续任何改动都不能把它交易掉**（V3 把它打到 0.0060，那条路已封）。

**3. V2 的名次从第 7 跳到第 3，而且它握着全场最高的 raw `pds`。**
raw `pds`：V2 **0.7857** > V8 0.7500 > V5/V6 0.7143 > A 0.5179；榜首 0.820。
旧尺子把 V2 判成失败（0.0922，倒数第二），是因为它的 `mse` raw 3.4226 在退化基线下
被重罚；官方刻度下 mse 反正是 0，这个罚款消失，V2 的 pds 优势就露出来了。
**V2 的三个改动（量化器 / top1000 / 去面板均值）里藏着 +0.0836 scaled pds（折 avg +0.0139）**，
但当初三个一起改（这正是 F29 单旋钮纪律的起因），不知道是哪一个买来的。→ 待做的三分解。

### 为什么会错：`from_baseline` 的 0 端不是同一个东西

`from_baseline` = $(u-b)/(r-b)$。我们的 $b$ 是**自建退化基线**，官方的 $b$ 是**官方 generic 基线**，
两者 raw 在 `mse` 上差 2.6 倍。换掉分母之后所有比值都变了，
**而我一直在拿换过分母的比值去和榜首用原分母算出的比值比。**
「同一标尺」这四个字我在 F31 里只保证了变体之间，没保证与榜首之间。→ 进 [T9](06-traps.md)。

### 仍未对齐的两处（不在本条范围内）

官方 $b$/$r$ 是在 **300 扰动 × 3 context** 上测的，我们的 raw 在 **8 × 1** 上测。
这条由 E30 负责。另外 E29 的本地 split-half anchor（在我们自己数据上测 $r$ 端）
启动后没产出（`anchor_splits.parquet` 不存在），故 `scale_local_anchor` 在 `rescore.json` 里是空的。

证据：`experiments/E29-official-baseline/out/rescore.json`
（`scale_official_scraped` / `from_baseline_degenerate_base` / `frozen_scale` / `mse_audit` 四节）。

## F33 · `lfc_nmae` 的瓶颈是**符号不可迁移**，不是基因覆盖；源并集净增益恒等于 `+0.000000` `实测`

完整证据链见 [E33/RESULT.md](../experiments/E33-source-union/RESULT.md)。[F28](#f28) 有三个具体错误：

| 量 | F28 说 | 实测 |
|---|---|---|
| 被打分的基因集 | 18,080 | 18,080 是 `var` 长度；CPM gate 是 **10,779**；而 `nmae` **自己的** gate 是逐扰动的 real 侧显著集（`p_adj<0.05` ∧ lfc 有限 ∧ 剔除靶基因自身），本面板均值 **570** 个基因 |
| K562GW 覆盖 | 7,481 / 18,080 = **41%** | **7,016 / 10,779 = 65.1%** |
| 指标在几个扰动上求均值 | 8 | **7** —— MAT2A 的 gate 只有 5 个基因 < `min_gate_size=10`，被指标整体省略 |

### 三个杀手事实（任何一条单独成立即足以否决）

**1. 并集在指标 gate 内新增 0 个基因，8/8 个扰动无一例外。**
四个 `*Essential` 源都是必需基因筛（2,057–2,393 个靶），本届 8 个扰动里只有 MAT2A 是
必需基因 —— 而 MAT2A 恰好是唯一被 `min_gate_size` 省略的扰动。Essential 源为它新增的
2,097 个基因全在显著集之外。CD4+T 源是空的（`log_fc.shape == (0, 10282)`）。
故 `V12 − V11 = +0.000000` raw，**逐扰动四位小数完全相同**。

**2. 跨系 lfc 符号一致率 51.1%–57.5%**（掷硬币 +1.1…+7.5 pp），10 个源对全部如此 ——
不是"K562GW 特别差"。按覆盖率本该排第一的填充源 HepG2 恰好**最差**：51.1%，
在最强 $\lvert\text{lfc}\rvert$ 十分位上 **49.5%，低于掷硬币**。
填充基因的 $\lvert\text{lfc}\rvert$ 中位跨源差 **2.4 倍**（0.1874–0.5007），尺度也不可比。

**3. 把源 lfc 铺进预测比沉默更差。** nmae 1.0004 → **1.0234**（6/7 个扰动变差），
越过 $b$ = 1.0009–1.0017 从好侧掉到坏侧，官方 avg **−0.0061 / −0.0067**。
而这是**只动 nmae** 的乐观下界：真打开 `lfc_all` 还会同时拖动 `expr_mse`
（lfc 空间 SSE 代理：预测 0 = 2189，GW-only = 3901，**1.78×**）与 `fid`。

MAT2A 那 5 个显著基因把"浆糊"说得最清楚 —— real 全为负，源几乎全为正：
`SHISA5 real −1.285 / GW +0.014 / Jurkat +0.621`，
`B4GALT4 real −0.437 / GW +0.486 / Jurkat +0.902`。

### 头寸很大，但当前不可达 —— 这是本条最重要的一句

我先前按 $0.178/6 = 0.0295$ 估这个成员的全部头寸，**估小了**。
完美利用 K562GW **现有**的 53.8% 幅值覆盖值 **+0.1245 / +0.1378** 官方 avg ——
因为该成员 $b\to r$ 的活跃跨度是 0.57–0.63 raw 单位，而榜首实际只拿到 0.11 raw 单位。

> **`nmae` 不是小头寸指标，是大头寸 + 当前零可达性。**
> 天花板 raw 0.5285 是 oracle（在覆盖处把 `lfc_real` 当预测），模型不可达。

**所以下一步不是加源，是找一个符号可迁移的源或估计量。**
本仓库现有的五个 nadig2025 细胞系 + 空的 CD4 源里没有这样的东西；
Replogle 2022 的 K562+RPE1 成对结构与 Jiang 2025 六系都**还没被当作 lfc 符号源验证过**。

⚠️ 证据等级：全部是离线算术复刻，**未跑 build / scorer / 未写任何 `.h5ad`**。
校准凭据：同一离线模型算 V8 自己得 `1.001036`，官方打分器实测 `1.000361`，
差 **+0.000675** —— 比要判定的效应（+0.023 恶化、+0.000 并集增益）小两个数量级。

## F34 · V13：`pds` raw 0.7500 → **0.8750**（超过榜首 0.820），但 `reach` 跌到无技巧点**以下** `实测`

单旋钮：给 `design_cells` 传 `lfc_all` —— 集外 top-550 的去面板均值 lfc（$\lambda_{\text{off}}$ = 1.0），
召集集那 288 个 lfc 逐位不动。完整合同与逐成员预测见
[E34/SPEC.md](../experiments/E34-pds-decouple/SPEC.md)（**build 前**写死），
实测见 [E34/RESULT.md](../experiments/E34-pds-decouple/RESULT.md)。

| 指标 | V8 raw | V13 raw | V8 lo | V13 lo | Δlo |
|---|---|---|---|---|---|
| `pds_cosine` | 0.7500 | **0.8750** | 0.5855 | **0.8782** | **+0.2927** |
| `direction_fidelity` | 0.4919 | 0.5023 | −0.0453 | −0.0092 | +0.0361 |
| `sig_jaccard` | 0.048234 | 0.048247 | 0.0769 | 0.0770 | +0.0000 |
| `expr_mse_capped_norm` | 1.0819 | 2.2208 | 0.0000 | 0.0000 | +0.0000 |
| `lfc_nmae` | 1.0004 | 1.0115 | 0.0009 | −0.0168 | −0.0177 |
| `direction_reach` | 0.1482 | **0.0250** | 0.1110 | **−0.0241** | **−0.1352** |
| **avg_score** | | | **0.1215** | **0.1508** | **+0.0293** |

官方 hi 端同向：0.0848 → 0.1069（+0.0221）。**两端都赢 ⇒ V13 取代 V8 成为当前最优。**
`pds` raw 0.8750 > 榜首 0.820，scaled 0.8782 > 榜首 0.708 ——
连同 `sig_jaccard`，我们现在在**两个**成员上高过榜首。

⚠️ 这是一笔**交易**，不是免费收益：买 `pds`（+0.2927），卖 `reach`（−0.1352）。
而且按 [T13](06-traps.md#t13) 修正后的无技巧点 **0.0355**，
V13 的 reach raw 0.0250 **跌到了无技巧点以下**（也低于官方 $b$ = 0.047–0.097）——
不是"变差"，是"比什么都不做更差"。

### 两个机制更正（这是本条比分数更重要的部分）

**① `direction_reach` 的排序不在 `r_set` 上，在全部非零 lfc 的 $\lvert\text{lfc}\rvert$ 上。**
SPEC §1 写「显著性只加在 `r_set` ⇒ reach 结构上不动」，错。
集外 550 个去均值 lfc 里有一部分幅度**大于**召集集成员，挤进纯前缀；
它们的符号来自跨系不可迁移的源（[F33](#f33)：一致率 51–57%），纯度立刻破，$k^\ast$ 崩。
`cap_probe.py` 把这条从推测变成实测：**V13 在 8/8 个扰动上都有
$\max\lvert\text{off}\rvert > \min\lvert\text{call}\rvert$**（例 MAT2A：1.017 > 0.782）。

`build_v3.py` 的 docstring 三周前就写过同一句话（「去面板均值让排序不再跟源侧置信度对齐，
纯前缀立刻崩掉」）。**我读过它，却以为它只适用于"改写召集集"的情形。**
它适用于任何进入 $\lvert\text{lfc}\rvert$ 排序的东西。

**② 声明的召集集 ≠ 打分器认定的显著集。**
`sig_jaccard` 动了 $1.3\times10^{-5}$。打分器对**预测出来的计数**自己跑 Wilcoxon，
判定线薄到 $d_{\text{crit}} = 0.0278$（[01-scoring.md](01-scoring.md)），
给 550 个基因加一阶矩就会让少数几个越线。
量级可忽略，但方向上说明 **`jac` 是有敞口的，不是锁死的** ——
而它是我们唯一的独有优势，**此后每次改动都必须逐位核对，不能假定**。

### 一个被隐藏的代价：`mse` 的期权被关掉了

`mse` raw 1.0819 → **2.2208**。官方刻度上不花钱（`clamp_low=0`，V8 已在地板），
所以 SPEC 的"免费赌注"预测成立。但 [07-roadmap](07-roadmap.md) 的 P4 是
"raw 须跌破官方 $b \approx 0.9985$"，V13 把这个距离从 **4.7%** 拉到 **122%**。
**"免费"只在"永远不打算拿 mse"的前提下成立。** 这一项 SPEC §4 没写到。

### 预测命中 1/6 —— 这正是预注册的价值

6 个成员里只有 `mse` 那条预测对了。`avg` 的方向对了，逐成员的理由几乎全错。
**如果没有在 build 前写下逐成员预测，我会把 +0.0293 当成"机制验证成功"收下。**
`score_v13.py` 打印的 `GO` 也只看了 `avg_lo`，漏了停止条件 ——
判定代码本身必须把停止条件编码进去，不能只算总分。
