# 02 · 实测发现

编号的事实清单。每条标注**来源**与**溯源等级**：

- `官方` — Arc 官方页面 / metric spec / `manifest.json`
- `实测` — 本机在官方 `controls.zip` 上跑出来（Apple M1 Pro / 16 GB / 无 CUDA，2026-08-27）
- `推算` — 由官方 + 实测做算术推出

工具版本：`cell-eval2` 0.16.0 · `vcc-cli` 0.1.0 · numpy 2.5.2 · scipy 1.18.1 · anndata 0.13.3

---

## F1 · 数据包逐项与规格吻合 `实测`

| 核对项 | 实测 | 规格 |
|---|---|---|
| 矩阵形状 | 18,400 细胞 × 18,533 基因，CSR float32，109,910,478 个非零 | ✓ |
| 基因顺序 | 三个文件都与 `gene_names.csv` **逐位相同** | ✓ |
| 数值 | 全整数，min 1，max 977 | ✓ raw counts |
| 测序深度 | 中位 **20,109** · 均值 21,134 · [3,275, 52,420] | ✓「median ~20,000」 |
| 每细胞检出基因 | 中位 6,147 · 均值 **5,973** | — |
| 对照 guide | 46 条，各恰好 400 细胞 | ✓ |
| 待预测基因 | 300 行，无重复，全部在基因表内 | ✓ |

来源：`experiments/E04-official-parity/` 的加载步骤。

---

## F2 · 发布的对照细胞就是打分用的比较组 `官方`

`manifest.json`：

```json
"per_context": { "A": { "control_cells": 18400, "ground_truth_cells": 138400, "n_ntc_ids": 46 } }
```

$$138{,}400 \;=\; 300\times400 \;+\; 18{,}400$$

**这一条把后面所有事情解锁了。** 对照分布、5 CPM 筛选门、多重检验的基因全集，
全部可以在本地精确重建。

---

## F3 · Wilcoxon 秩和对固定对照组有闭式 `实测`

$$\psi_g(v)=\#\{c: x_{cg}<v\}+\tfrac12\#\{c: x_{cg}=v\},\qquad U_g=\sum_{i=1}^{400}\psi_g(v_i)$$

与 `scipy.stats.mannwhitneyu` 逐位相等：

| 情形 | scipy 的 U | ψ 求和 |
|---|---|---|
| 正常随机分布 | 3,862,696.0 | 3,862,696.0 |
| 整体偏移 | 4,735,458.5 | 4,735,458.5 |
| **400 个细胞全相同** | 4,771,000.0 | 4,771,000.0 |

3/3 精确相等。来源：[E01](../experiments/E01-psi-identity/)。

**推论**（`推算`）：六个指标全部只经过每基因两个标量。细胞级联合分布对分数零贡献。

---

## F4 · 显著性阈值极薄 `推算`

$$d_{\text{crit}}=\frac{1.96\,\sigma_{\text{tie}}}{n_1n_2}=0.0278 \qquad (\sigma_{\text{tie}}=104{,}477)$$

平均对照分位数只要移动 **2.78 个百分点**就判显著。

---

## F5 · 显著性与方向完全解耦 `实测`

固定一阶矩（真实基因 `SELENOT`，context A，对照均值 62.54 CPM，5,885/18,400 个对照细胞检不到它，
目标均值锁在 78.18 CPM 即 lfc = +0.322），只改组内分布形状：

| t | 非零细胞 | 均值 | $d$ | $p$ | 判定 |
|---|---|---|---|---|---|
| 0.00 | 400 | 78.18 | +0.1614 | 2.81e-29 | 显著 |
| 0.60 | 250 | 78.18 | +0.0944 | 4.61e-11 | 显著 |
| 1.05 | 195 | 78.18 | +0.0348 | 1.51e-02 | 显著 |
| **1.30** | 174 | 78.18 | +0.0043 | **7.66e-01** | **不显著** |
| 1.70 | 148 | 78.18 | −0.0391 | 6.32e-03 | 显著 |
| **3.00** | 100 | 78.18 | **−0.1312** | 5.20e-20 | 显著 |

**关键观察：t = 3.00 时均值仍然向上（lfc = +0.32），但检验读到的偏移变成了负的。**
检验读**秩**，倍数变化读**均值**，两者可以指向相反方向。

来源：[E02](../experiments/E02-dispersion-dial/)。完整 12 行表见该目录。

**推论**：每个基因上有**三个可独立指定的旋钮** —— 进不进显著集、报涨还是报跌、报涨多少。

---

## F6 · 可检出性门槛：用 BH 有效阈值算 `实测`

### 阈值必须用 BH 的，不是单基因 α=0.05

真实打分器在**每个扰动内**、对 gate 内 $m=9{,}929$ 个基因做 Benjamini–Hochberg。
在解点 $k=|R_p|\approx288$ 处，$p$ 值截断为

$$p_{\text{BH}} = \frac{\alpha k}{m} = \frac{0.05\times288}{9929} = 0.00145
\quad\Longrightarrow\quad z_{\text{BH}} = 3.184$$

用单基因 α=0.05 的 $z=1.96$ 会把门槛**系统性低估**。而且这不是纯尺度变换 ——
低门槛基因被推高更多（ψ→lfc 关系饱和）：

| 分位 | **BH（正确）** | α=0.05（错） | 比 | BH 下的倍数变化 |
|---|---|---|---|---|
| 5% | **0.0985** | 0.0220 | 4.48× | 1.07× |
| 25% | **0.1845** | 0.1021 | 1.81× | 1.14× |
| 50% | **0.2980** | 0.1869 | 1.59× | **1.23×** |
| 75% | **0.5320** | 0.3693 | 1.44× | 1.45× |
| 95% | **1.2586** | 1.0679 | 1.18× | 2.39× |

$$\text{动态范围} = \frac{p_{95}}{p_{25}} = \mathbf{6.8\times}\qquad (p_{95}/p_5 = 12.8\times)$$

一类错误噪声底从 3.31% 降到 **0.07%** —— 这正是 BH 控 FDR 应有的效果。

> **更正记录**：曾先后报过 33×（用 $p_{95}/p_5$，分母落在噪声底上）和 10.5×（用了错的 α=0.05 阈值）。
> 现值 **6.8×** 用的是打分器真实的 BH 有效阈值。

### 推论，以及它的边界

$$R_p = \underbrace{\{h:\ \text{生物上真的变了}\}}_{\text{未知，需要建模}} \cap \underbrace{\{h:\ \text{检得出来}\}}_{\text{官方已经给你了}}$$

**但可检出性只是边际修正项，不是策略** —— 见 [F15](#f15) 的理论确认：
只按门槛排序的「零生物学」策略理论上限只到 $h=0.130$，而追平榜首需要 $h\ge0.134$。

来源：`scripts/export_app_data.py`，全量 9,929 基因，19.6 s，含显式 lfc=0 评估。

---

## F7 · 「预测什么都没变」会被判 99.3% 的基因「变了」 `实测`

一个把对照平均谱复制 400 遍的退化提交（lfc 中位绝对值 **0.0052**，即「什么都没变」），
被判 **9,863 / 9,929 = 99.3%** 的基因「变了」。

**这是解码器故障，不是选集故障。** 明明预测「没变」，却报了 9,863 个「变了」。

---

## F8 · 由此白拿参考显著集的大小 `推算`

既然任何零方差的 400 细胞提交都会吞掉几乎整个筛选门（F7），那么官方的 `jac` 基线满足

$$b_{\text{jac}} \;\approx\; \frac{|R_p \cap \hat R|}{|R_p \cup \hat R|} \;\approx\; \frac{\mathbb{E}|R_p|}{9{,}863}$$

官方公布 $b_{\text{jac}} = 0.021\text{–}0.037$，代回：

$$\boxed{\ \mathbb{E}|R_p| \;\approx\; 200 \sim 375\ \text{个基因（中点约 288）}\ }$$

**这个数直接给出 `fid`/`jac` 联立最优点上该报多少个基因。** 本来打算花一次线上提交去套。

⚠️ 依赖「官方基线也是退化提交」这一步。与 `mse` 基线 ≈ 1（官方 0.986–0.992，
即「emit control unchanged」）一致，故置信较高，但仍是推算。

---

## F9 · 三个细胞系是跨组织的，不是近亲 `实测`

basal log-pseudobulk 两两 Pearson：**A–B 0.7420 · A–C 0.6854 · B–C 0.7927**。

筛选门也是细胞系特异的：A 9,929 / B 9,626 / C 10,124，交集 7,991、并集 11,961
—— 约 4,000 个基因只在部分细胞系里被检验。

**推论**：跨细胞系迁移是真问题，Stage 1 不能靠「假装三个细胞系一样」蒙过去。

---

## F10 · Stage 2 构造器是无损的 `实测`

| | |
|---|---|
| 意图让 250 个基因显著 → 实际显著 | **250 / 250** |
| 召回 / 精确率 | 100% / 100% |
| 假阳性 | **0** |
| 方向一致率 | 100% |
| lfc 中位绝对误差 | 6.1e-4 |
| 单组耗时 | **0.28 s** |
| 每细胞非零值 | 5,998（真实数据 ~5,973，天然合规） |

完美 Stage 1 假设下送进评分公式，得到的正好是「完美复现真实数据」的理论上限：
`jac` scaled **2.62** · `fid` **1.62** · `nmae` **1.66**。

来源：[E03](../experiments/E03-decoder-exactness/)。

---

## F11 · 与官方打分器逐基因一致 `实测`

`cell-eval2` 0.16.0，preset `vcc2026`，scanpy 后端，CPU：

| | 官方 | 我们 |
|---|---|---|
| 门内基因数 | 9,929 | **9,929（完全一致）** |
| 官方 DE 表 | 29,787 行 | — |
| `log2_fold_change` 最大绝对差 | — | **1.0e-5**（float32 存储精度） |
| `log10(p_adj)` 中位绝对差 | — | **0.0000** |

显著集大小：

| 组 | 官方 | 我们（含并列校正） | 我们（未校正） | 对称差 |
|---|---|---|---|---|
| ABCD1 | 251 | **251** | 249 | **0** |
| ACLY | 250 | **250** | 249 | **0** |
| ADNP | 251 | **251** | 250 | **0** |

官方六指标同时给出：`de_wilcoxon_sig_jaccard` = 0.249377（三组相同，= 100/401，
与我们故意设计的 100 个重叠吻合）；`direction_fidelity` = 0.49004 / 0.466135 / 0.517928；
`direction_reach` = 0（方向随机设，纯度到不了 0.9，行为正确）。

来源：[E04](../experiments/E04-official-parity/)。

---

## F12 · 41 倍加速及其机制 `实测` + `推算`

| | 3 个扰动（单侧 DE 表） | 全 panel（300 × 3） |
|---|---|---|
| 官方 `cell-eval2`（scanpy CPU） | 296.85 s | **52 小时** |
| ψ 复刻 | **7.66 s** | 60 min 单核 / **6 min 十核** |

实测比 **38.8×**。机制（op 计数）：

| | 每扰动每基因的比较次数 |
|---|---|
| 官方 | $(n_1{+}n_2)\log_2(n_1{+}n_2) = 18{,}800\times14.2 = \mathbf{266{,}960}$ |
| 我们 | $n_1\log_2 n_2 = 400\times14.2 = \mathbf{5{,}668}$ |

理论比 **47.1×**；一次性建表成本（$2.59\times10^9$ 次比较，实测 **2.0 秒**）在 900 组上摊销后
只占我们总工作量的 **4.9%**，故全 panel 摊销后趋近 **44.8×**。

---

## F13 · 提交矩阵的密度与内存 `实测`

| | |
|---|---|
| 每细胞非零值 | 5,998 |
| 总存储条目 | 2.16×10⁹ = 上限（4.75×10⁹）的 **45%** |
| 原始内存 | **17.1 GB** → 装不进 16 GB |
| 按组流式写 | 每块 400 × 5,998 ≈ 19 MB，常驻 < 100 MB |
| 生成整份提交 | **4 分钟**（900 × 0.28 s） |

**迭代时不需要写盘** —— 自评可以逐组在内存里算完就丢。只有真要提交时才落盘。
这是「10 分钟一轮」成立的前提。

清零 <1 CPM 基因几乎不省 nnz（5,998 → 5,986），因为稀疏度来自自举的真实细胞支撑集。

---

## F14 · 收益算术：一切走 $h$ `理论确认`

**定义** $h = |R_p \cap \hat R_p| / |R_p|$，即我们能恢复的参考响应基因比例。

### `fid` 不是自由的 —— 它由 $h$ 完全决定

`fid` 数的是「你报的基因里，预测 lfc 符号与**参考的** lfc 符号一致」的比例。
对一个**没有真实效应**的基因，参考测到的 lfc 是噪声主导 → 符号是掷硬币 → **不可预测**。
所以假阳性上的方向准确率被结构性钉在 $\approx 0.5$：

$$\mathrm{fid} = a_{\text{true}}\cdot h + 0.5\cdot(1-h) = 0.5 + (a_{\text{true}}-0.5)\,h$$

> **更正记录**：曾把 $a_{\text{false}}$ 当自由参数（取 0.55–0.85），据此声称
> 「$h=0$、只把方向做对就能到 1.20× 榜首」。**那是错的** —— $h=0$ 时 $\mathrm{fid}$ 必然 $=0.5$。

### 修正后的收益表

固定 `pds` raw = 0.820、`mse` raw = 0.959（榜首水平），$a_{\text{true}}=0.90$，
`reach` $\approx 0.8h$，`nmae` $=1-0.5h$：

| $h$ | fid raw | fid scaled | jac scaled | **overall** | vs 榜首 |
|---|---|---|---|---|---|
| 0.00 | 0.500 | −0.043 | −0.078 | 0.0891 | 0.47× |
| 0.10 | 0.540 | 0.090 | 0.064 | 0.1638 | 0.86× |
| **0.134** | 0.554 | 0.135 | 0.115 | **0.1899** | **1.00×** |
| 0.20 | 0.580 | 0.223 | 0.222 | 0.2411 | 1.27× |
| 0.30 | 0.620 | 0.357 | 0.399 | 0.3215 | 1.69× |
| 0.40 | 0.660 | 0.490 | 0.597 | 0.4056 | 2.14× |
| 0.50 | 0.700 | 0.623 | 0.823 | 0.4941 | 2.60× |

$$\boxed{\text{追平榜首需要 } h \ge 0.134}$$

---

## F15 · 三个理论确认（不需要新数据） `理论确认`

### ① $h$ 的物理上限：0.570

用官方 replicate 锚点反推：$r_{\text{jac}}=0.399$，而 $\mathrm{jac}=h/(2-h)$，故

$$h_{\text{replicate}} = \frac{2\times0.399}{1.399} = \mathbf{0.570}$$

**连同一细胞系、同一扰动、真实数据对半劈开，显著集也只重叠 57%。** 这是噪声上限。

| 目标 | 需要 $h$ | = replicate 水平的 |
|---|---|---|
| 追平榜首 | 0.134 | **23%** |
| 1.27× 榜首 | 0.20 | 35% |
| 1.69× 榜首 | 0.30 | 53% |

### ② 「零生物学」策略的理论上限：$h = 0.130$，赢不了

只按可检出性排序、报 $K=288$ 个基因。关键化简：先验响应概率 $\pi$ 在分子分母里**约掉**，
所以 $h$ 只依赖效应量尺度 $s$。设 $|\mathrm{lfc}|\sim\mathrm{Exp}(s)$，
用实测 MDE 分布 + 约束 $\mathbb{E}|R_p|=288$ 定出 $\pi$，扫遍可行区间：

| $s$ | 蕴含 $\pi$ | 理论 $h$ | jac scaled |
|---|---|---|---|
| 0.15 | 0.148 | **0.130** | 0.11 |
| 0.25 | 0.096 | 0.091 | 0.05 |
| 0.40 | 0.070 | 0.069 | 0.02 |
| 0.60 | 0.057 | 0.058 | 0.00 |
| 0.90 | 0.049 | 0.050 | −0.01 |
| 1.40 | 0.043 | 0.044 | −0.02 |

$h\in[0.044,\,0.130]$，最好情况 overall $=0.1867 = 0.98\times$ 榜首。**赢不了。**

**机制**：多数真实效应（$s\approx0.4$–0.6）本来就远高于多数基因的门槛，
可检出性只在小效应尾巴上起作用。

> **更正记录**：曾把「E05 零生物学基线」列为可能直接夺冠的 go/no-go 门。**理论上限就到不了。**
> E05 降级为理论校准检查。

### ③ 因此真正的问题只有一个

$$\text{跨细胞系迁移能恢复的响应基因集，是否达到同系重做实验的 23\% 以上？}$$

这个问题**完全用公开的多细胞系 Perturb-seq 数据就能回答** ——
不需要官方数据、不需要提交额度。见 [E07](../experiments/E07-source-coverage/)。
